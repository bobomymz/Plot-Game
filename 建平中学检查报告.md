2026.9.6
  ---
  # 总体评价：
  建平中学的主体设计完成度很高，核心玩法链（老吴杂物室→14班电脑→忻老师→Harsh→橘猫→食堂煤气）全部实装。下面是发现的缺失项，按严重程度分级。
  ---
  一、确认缺失（在 建平中学.js 里找不到对应逻辑）

  1. 废弃小楼廊桥门锁（应需钥匙串）
  设计稿明确：3F 廊桥通图书馆的门"平时锁着，用老吴钥匙串解锁"。
  实现中 建平-废弃小楼-3F 直接有"经廊桥去弘渊楼"选项，无任何钥匙串门控。

  2. gasIndex 系统不完整
  建平-食堂-后厨 的 onEnter 里写了 vars.gasIndex += 20，但全文件没有任何地方用 gasIndex 触发 QTE 或死亡。
  设计稿要求：超阈值 → QTE 时间压力 → 再超 → 死亡。目前只是一个只增不用的变量。

  3. 刘冠宇被间接害死的 ch 联动未实现
  设计稿：玩家进入食堂时 ch 较高 → 丧尸闯进来咬死刘冠宇（救忻老师与救刘冠宇的两难冲突）。
  实现中食堂只判断 dd >= 3 && !_gasValveClosed 来置 _liuCorpse，没有 ch 触发路径。

  4. jpPengAtPiano(vars, 2) 缺少挹芬楼东侧已清门槛
  设计稿：仅 _yifenEastCleared 后，挹芬楼休息区钢琴才进入候选。
  jpPengAtPiano 函数只检查 hh === 13 || hh === 16 和 _pengPiano，没有检查 _yifenEastCleared。
  如果 reactive 规则也没做这个门槛，玩家未清场时会看到彭奕宸出现在危险走廊里弹琴。

  ---
  二、需要在 core.js 确认的（建平中学.js 无法自证）

  5. Harsh 追踪的 reactive 规则
  设计稿：reactive 规则 harsh-chase（triggerKey: Math.floor(gameMinutes / 3)），每 3 分钟 _harshLag - 1。
  建平中学.js 里没有这段逻辑，应在 core.js 的 _reactive.rules 中，需确认存在。

  6. _harshCaught 全局触发器
  设计稿：追上重定向走全局触发器 _harshCaught，应在 core.js _globalTriggers 中。

  7. _pengPiano reactive 规则
  设计稿：hh == 13 / 16 时随机 roll _pengPiano（1=远翔楼圆厅，2=挹芬楼休息区带门槛，3=音乐教室）。应在 core.js。

  8. mixedMemorySet 变量声明
  "看B站"场景里调用 vars.mixedMemorySet.add("腐烂尸城")，如果 mixedMemorySet 未在 _variables 中声明为 Set，运行时会报错。

  9. 建平专属变量注册完整性
  gasIndex、mercuryLoad、hasCSGun、hasCracker、hasSnackCookie、hasHamSausage、hasMultimeter、hasPipelineMap、hasInnerLining（整数）、各
  _harsh*、各 _yifen*、_laowuKilled、_pengPiano 等是否全部在 _variables 注册，需核查一遍。

  ---
  三、设计稿明确说"暂缓/待定"的（属于已知未实现，正常）

  - 化学实验室合成台具体配方（暂缓）
  - 废弃小楼除 CS 枪外的具体道具
  - 郑楷地铁支线
  - 地铁 11→6 换乘路径
  - 济美楼心理教室内容
  - 复旦江湾全剧情（现仅占位文本）

  ---
  四、小瑕疵
| 问题 | 位置 | 说明 |
| --- | --- | --- |
| 挹芬楼1F/济美楼1F 饮料机 | 两处饮料机节点 | 只有"离开"选项，无喝饮料交互 |
| 廊桥只实现了 3/4/5F | 致真楼↔远翔楼 | 设计稿写"3/4/5 层廊桥"，实现了三层✅；废弃小楼只有3F廊桥，图书馆也只接3F，设计稿说的是"3楼廊桥通图书馆"✅ |
| 建平‑食堂‑刘冠宇   天黑后刘冠宇对话 | 刘冠宇节点 | Day≥2 煤气泄漏时刘冠宇说"别喝水"但没有检查 `_liuCorpse`，死人还会对话 |

  ---
  建议优先修复顺序：
  1. 先去 core.js 确认 Harsh 规则、全局触发器、_pengPiano 规则、mixedMemorySet 这几个基础机制（5-8）
  2. 修 gasIndex QTE 逻辑（或直接把 gasIndex 改为简单的"进去就战斗"，去掉那行 +=20）
  3. 废弃小楼廊桥加钥匙串门控
  4. jpPengAtPiano 或 reactive 规则里加挹芬楼东侧门槛