# 忻老师章节（复旦江湾）完成度核查报告

> 生成：2026-09-24 ｜ 依据：`忻老师故事线.md`（蓝本）+ `docs/区域方案-复旦江湾.md`（阶段1区域方案）
> 方法：全库源码审读 + 审计回归 + 新增分支覆盖探针 `tools/xin_chapter_check.js`

## 一、结论

**代码层：完成度约 100%（阶段2骨架 + 阶段3正文全部落地），功能上可通关。**
2×2 分支矩阵、a₁/a₂ 双出示窗口、药丸延迟引信、missable 全部经**运行时实走验证**（新探针 55/55 通过）。

**文档层：3 处未收口 → 已全部收口**（区域方案 DoD 自列的收口项）：
1. `人物档案.md` **无忻老师条目** → ✅ **已补 016 条**（2026-09-24；严格只写代码已落地部分，未实装项集中在条目文末清单）。
2. `人物档案.md` 王知筠 305 节仍标「未实装/占位」——✅ **已修**（2 处，事实性过期）。
3. `CLAUDE.md` / 差异矩阵未补 `_phoneOrigin` —— ✅ **已定调并落地**：差异矩阵 = `.claude/skills/area-story-design/SKILL.md`（追加复旦江湾行）；`_phoneOrigin` **不**升格为通用机制表行，改在 CLAUDE.md「照明分级」条内补一句"手机有多处拾取点时必须同步写 `_phoneOrigin`"防 b 链静默失效。

**另发现 1 处系统性工具缺陷（已修 + 已根治）**：9 个审计脚本的文件清单漏载 `story/复旦江湾.js`，
导致本章此前**从未被任何审计覆盖**——"全绿"对本章是**假绿**。本次除补清单外已根治：新增
`tools/story_files.js`（唯一权威 = 解析 `index.html`），9 个脚本改为调用它。

## 二、审计工具缺陷（本次修复）

| 脚本 | 漏载后果 |
|---|---|
| `condition_audit.js` | 本章字符串条件**从未求值**（两个 0 是"没扫到"，不是"没问题"） |
| `scene_fn_selftest.js` | 函数式 text/choices/nextScene 未实调；并误报 `复旦江湾-校门`/`建平-教师小区门口`「不存在」 |
| `overnight_shelter_audit.js` | 本章 `_visit` 键、过夜跳转未做悬空校验 |
| `qte_report.js` | 本章 2 处场景级 + 1 处选项级 QTE 未计入分布 |
| `combat_penalty_report.js` | `结局-变了的忻老师` 等失败点未计入 |
| `combat_stamina_feedback_audit.js` / `sprint_away_audit.js` / `bottle_scan.js` / `bottle_pickup_selftest.js` | 本章相关统计缺失 |

**第一步修复**：9 个脚本的 `FILES` 统一补 `'story/复旦江湾.js'`（插在 `建平中学.js` 之后，与 `index.html` 顺序一致）。

> ⚠ **根因**：这些脚本各自硬编码一份 `FILES` 清单，新增剧情文件时**不会自动跟上**，
> 且**不报错**（vm 里少加载一个文件，只是少一批场景 → 少一批问题 → 更容易"全绿"）。
> `bridge_node_check.js` / `mercury_engine_e2e.js` / `mercury_leak_guard.js` 从 `index.html` 解析加载列表，天然免疫。

**第二步根治（2026-09-24 落地）**：新增 `tools/story_files.js` —— 唯一权威 = 解析 `index.html` 的
`<script src="story/*.js">`；解析结果异常（< 10 条）时回退内置清单**并打警告**（不让审计静默变空跑）。
上述 9 个脚本已全部改为 `const FILES = require('./story_files').list();` 形式；
`sprint_away_audit.js` 因 CORE/REST 两段加载，改用 `.filter()` 剔除 CORE 后重建 REST。
**今后新增剧情文件只要挂进 `index.html`，审计自动纳入——不再需要人工同步 N 份清单。**

## 三、回归读数（已含本章）

| 审计 | 期望 | 实测 |
|---|---|---|
| `condition_audit.js` | 两个 0 | ✅ 0 / 0 |
| `scene_fn_selftest.js` | 0 处异常 | ✅ 0（场景 1437，text 4991 / onEnter 4921 / 跳转 21380） |
| `overnight_shelter_audit.js` | 悬空 0 / 失效 0 | ✅ 0 / 0（过夜安全屋 21 个） |
| `sprint_away_audit.js` | 0 处 P0 | ✅ 0 |
| `mercury_selftest.js` | 37/37 | ✅ 37/37 |
| `mercury_engine_e2e.js` | 20/20 | ✅ 20/20 |
| `mercury_leak_guard.js` | 0 处 P0 | ✅ 0（42 次渲染） |
| `unarmed_fight_selftest.js` | 34/34 | ✅ 34/34 |
| `bag_volume_selftest.js` | 33/33 | ✅ 33/33 |
| `bottle_pickup_selftest.js` | 72/72 | ✅ 72/72 |
| `stamina_report.py --selftest` | 22 条 | ✅ 通过 |
| `stamina_telemetry_selftest.js` | 9/9 | ✅ 9/9 |
| `bridge_node_check.js` | 22/22 | ✅ 22/22 |
| `area_check.mjs 复旦江湾` | 三连全绿 | ✅ lint E=0 / graph OK / chain OK |
| **`xin_chapter_check.js`（本次新增）** | 全通过 | ✅ **55/55** |

**补记（2026-09-24 收尾）**：`area_check` 原先报的 lint **W=2 已根治 → 现 W=0**。两处
（`忻老师家-翻窗双逃`、`结局-变了的忻老师`）都是**假阳性**：入边来自函数式多分支 `return`，
而 lint 只用 `mkVariants()` 的 7 组固定状态实走 `choices()`，取不到那些组合分支；且 `areaFilter`
模式只加载单文件，跨文件入边（建平中学.js → 结局-变了的忻老师）必漏。
已在 `lint_story.mjs` 加「3.5 入边补账」pass：从 `qte`/`onEnter`/`choices` 的**函数源码字符串字面量**
里提取、命中真实场景 id 者补记入边，**全量跑、不受 areaFilter 影响**。修后全图仍报 16 处真孤立、
`E=0`——没有把真问题一起盖掉。

## 四、分支覆盖（新探针 `tools/xin_chapter_check.js`，55 断言）

无头加载**真实 engine.js + 全部剧情文件**，按引擎语义实走章节（`_lastScene` 先写后更新、`_visit` 先自增后渲染、
`showCondition` 另行过滤），逐步应用 `onEnter` / 选项 `effect`。

| # | 路径 | 结果 |
|---|---|---|
| 1 | ① !a !b 堵门牺牲 | `_xinOutcome=1`，`_xinScratched=true`，得车 |
| 2 | ② !a b 学生迟到目击 | `_studentsCalled=true`（-5电），`_xinOutcome=2`，得车 |
| 3 | ③ a₁ !b 翻窗双逃 | `_xinKnowsTruth=true`，**出示不扣电**，`_xinOutcome=3`，`hurtByZombie=true`，汞 +10 |
| 4 | ④ a₁ b 学生救场全歼 | `_xinOutcome=4`，胜利挂 `combatDrain` 扣体力，**无人受伤** |
| 5 | a₂ 返程车程出示（该走 ③）/ 空口安慰陷阱 | 双向验证：a₂ 置真；空口安慰置假且不改状态 |
| 6 | 延迟引信 `jpXinFuse` | ③ dd>day 炸 / ④ dd>day+1 炸 / **给药不炸**；3F 走廊路由 `结局-变了的忻老师`；结局文案**无「汞」字**（不泄露机制） |
| 7 | missable | `_xinGone` 空车位（轮胎印）、上车选项消失、办公室空屋文案 |
| 8 | 整理整理给药 | 位置门控（建平中学可见 / 复旦章内不可见）、无药/已给不可见、④ 同样可给 |
| 9 | 仁济门诊药房第二入口 | 无药可见 / 持有藏起（防重复） |
| 10 | 章节全部 24 节点 × 四组合零选项扫描 | 无零选项节点 |

## 五、蓝本/方案对接点核对（10 条，逐条已验）

| 蓝本 §八 对接点 | 状态 |
|---|---|
| 1. 上车选项清空交通工具 + 门槛收紧 | ✅ 建平中学.js:795（清空）；门槛置 **hh<14**（方案修正，蓝本 hh<16 时间数学不够） |
| 2. `建平-前往复旦` 改写 / 占位删除 | ✅ → `复旦江湾-校门` |
| 3. 校园门口四分支回归 + 整理整理入口 | ✅ 建平中学.js:262 |
| 4. 物理办公室 `_xinTurned` 死亡分支 | ✅ 建平中学.js:1699-1707 |
| 5. 仁济门诊药房第二入口 | ✅ 仁济南院.js:694/740 |
| 6. 童涵春堂拾取不动 | ✅ |
| 7. `_phoneOrigin` 两个拾取点 | ✅ 全家原机 `"own"`；新达汇展示机 `"store"` |
| 8. 夜晚剧情.js 复旦防御分支 | ✅ 文案分支 + `过夜-复旦-车内` + 兜底 showCondition 排除复旦 |
| 9. ④ combatDrain + 失败跳共享死亡结局 | ✅ |
| 10. 新增机制同步 CLAUDE.md | ❌ **未做**（见遗留） |

## 六、遗留项（待拍板 / 未收口）

**文档（非代码问题）**
1. ✅ **已做**（2026-09-24）：`人物档案.md` 新增 **016 · 忻老师** 条目 —— 严格只写代码已落地部分；未实装项（全名/年龄/外貌/"复旦招生办联络人"身份/妻女细节）集中在条目文末"未实装（留待补充）"清单，等你补。
2. ✅ **已做**：差异矩阵 = `.claude/skills/area-story-design/SKILL.md`（此前以为的"独立文件"其实在 skill 内），已追加复旦江湾行。`CLAUDE.md` 判定**不做**通用机制表行，改在「照明分级」条内补一句防静默失效的提醒。
3. `story/尸潮安全屋.md` 仍是 v2 过期文档（已知，顶部已有提示）。
4. ✅ **已做**（2026-09-24 收尾）：`人物档案.md` 补 **017 · 蔡镜晓 / 018 · 彭奕宸 / 019 · 刘冠宇** 三条目（沿用 016 口径：只写代码已落地部分，未实装项集中列在文末等你补）；头部编号说明 / 实装状态 / 总表已同步。

**代码层可选增强（非缺陷）**
5. 14 张章节图仍为 `images/placeholder.png`（方案已声明由你生成 WebP）。
6. ✅ **已做**：审计脚本清单改为解析 `index.html`（`tools/story_files.js`，见第二节"第二步根治"）。
7. ✅ **已做**（2026-09-24 收尾，见第三节补记）：不再依赖"跑多组状态"，改为从函数源码字面量补记入边。

**明确不是 bug，勿改**
- 王知筠手机电量是**门槛不是货币**（出示不扣电）——设计意图。
- `建平-教师小区门口` 双态 `onEnter` 不设 `currentArea`（两侧落点都对）——方案已注「勿修复」。
- 家中→翻窗双逃是函数式路由——本身非缺陷；此前导致 lint 假报孤立，**已由 lint 修复消除**。
