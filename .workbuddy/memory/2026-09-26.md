# 2026-09-26 工作日志

## 灵海社活动室：环境基调订正（波波反馈）
- 原活动室文案写成了「旧机柜 / 闷了很久的灰味 / 水渍干了一圈又一圈」的破旧感，**与设定不符**：灵海社是去年翻新过的新教室，设备新。
- 改为：板材与胶水味、墙面白得发亮、新机柜线缆整齐、指示灯齐刷刷亮着；血迹/暴力痕迹照旧保留（白椅翻倒、椅面发黑一片、顺地砖缝拖到门口变淡——暗示人被拖走，不留未接的尸体节点），讲台上半杯水落了薄灰。
- 顺手修一处叙事矛盾：「建平-躲藏-灵海社活动室」写死「白板还静静立在原地」，但白板被踹过（_linghaiBoardKicked）后不成立 → 改为「没有人推这扇门」。
- 回归：condition_audit 0/0、scene_fn_selftest 0 异常、lint E=0、重名 ID 0、jp_bell 23/0、bridge 22/0、unarmed 34/0、bottle 72/0、mercury 37/0、xin 74/0、save_compat 27/0（bag 那条为已知旧档缺陷，非本次引入）。

## 泡面可堆叠化（全家货架）
- 需求：泡面改成背包可同时装多个（对齐维C）。**先盘点**：全库 8 处提到泡面，只有全家促销货架是物品型（世界库存 3 包）；樱桃苑床底/建平彭奕宸/张江洪金宝/上实南校·安居苑·安盛街·联华都是当场吃或纯文案，未动。
- 改动：`hasInstantNoodle:false` → `instantNoodle:0`（0~3，每包 1 格）；整理整理吃/丢改扣 1 包（文案「吃一包泡面（体力回满，N包）」/「丢一包泡面」）；全家拿取去掉 `!hasInstantNoodle` 限制、文案加货架余量；`utils.js` FOOD_GIFTS 换名 + `foodGiftChoices` 加数字扣减分支（布尔口粮仍整件给出）；`engine.js` fillMissingDefaults 加字段迁移（未 bump SAVE_VERSION，走反推迁移）。
- **踩坑（已修）**：迁移块初版写在补默认值循环【之后】→ 新字段先被填 0、卫语句恒假 → 老档那包泡面凭空消失（静默）。必须排在循环**之前**、且以旧键为准。已写进 skill 坑位表 + `noodle_stack_selftest.js` S5 源码顺序断言。
- 新增 `tools/noodle_stack_selftest.js`（64 断言，全过）+ 报告 `tools/泡面堆叠改造报告.md`。
- **待波波拍板**：3 包随身 ≈ 3 次「体力回满」，续航比改造前强（原先吃完得回全家再拿）。可选降为 +5 / 提高吃泡面耗时 / 维持现状。数值我未擅改。
- 回归全绿：noodle 64/0、condition_audit 0/0、scene_fn 0 异常、save_compat 27/0、lint E=0（W=113 与基线同）、重名 0、bag 32/0、bottle 72/0、unarmed 34/0、jp_bell 23/0、mercury 37/0 + e2e 20/0 + leak 0、bridge 22/0、xin 74/0、overnight 悬空 0、stamina 9/9 + 9/9。

## 高锦睿线索补全（WorkBuddy）
- 起因：玩家视角报告里 P1（喷泉广场 10-14 时间窗错过后整条高锦睿线消失）。波波指出贩卖机联动挂 `_metGaoAtMall`（见过才算，与当前时间窗无关），我原报告措辞不严谨已更正。
- 落地两层：① 樱桃苑物业楼-高锦睿-聊 当面补"去新达汇（开阔/有吃的/她家在那片）"——对上物业楼次日墙上"高某到此一游。下一站，新达汇"；② 上实南校-撤离成功 新增一次性选项 + 节点"上实南校-撤离成功-高锦睿"，小赵讲行为逻辑（坐不住/车修好就走/待不过半天/他是骑车你两条腿，去晚了早挪窝）。未动时间窗。
- **修掉真 bug**：`_metGaoAtMall` 此前从未在 `_variables` 注册（仅 onEnter set）；新 showCondition 引用即 ReferenceError→选项静默消失。已在 core.js:49 补声明（+1 行，stamina_report 锚点自测仍 ok）。
- 回归：condition_audit 0/0、scene_fn_selftest 0 异常、scene_id_dup 0、save_compat 27/27、lint E=0 W=113（无一指向改动）、sprint_away 0 P0。
- 遗留选项 B（未做，待拍板）：华为店加"错过窗后的店内相遇分支"，需给 聊 节点加第三种承接文案。

## 东明街道 · 玩家视角试玩（WorkBuddy，报告 tools/东明街道-玩家体验报告.md）
- 玩法：初始卧室起步，逐节点只读当前场景、选一个选项，不预读设计文档；覆盖 樱桃苑/民防/全家/安盛街(理发店·文具店·食品店·老头丧尸)/菜市场/益丰药房/地铁站入口，死 4 次。新达汇用旧报告跳过。
- **4 处 P0 必死假选择**：① 起床散步去喽（utils.js:610 `zombieOutsideHome=(dd==1||dd==3)&&hh%2==0`，8 点偶数即死）② 民防楼梯间"前进找紧急出口"三选项全死 ③ 东出口红色按钮→唯一选项"谁？"→死 ④ 全家"喝饮料"→死。
- **P1 机制不一致**：有手电反而堵死员工通道（需先 `_visit['全家便利店-饼干引路']`，无手电可"摸黑"安全走）；全家"丢零食引开"必死 vs 地铁站"丢水瓶引开"有效；文具店"仔细翻找"文案给饼干但 effect 只 set `hasCrumpledLeaflet`（安盛街:867-881）；`Math.random()<0.5` 直接判生死（樱桃苑:1287/1446）；日记本查看 50% 概率才出现（全家:252，唯一教"打腿"的路径）。
- **体验结论**：难度峰值错放在开局（信息最少时死亡最密）；体力被免费回满点稀释（全家面包/饭团 set strength:10），真实压力来自背包 3→4 格；最强叙事是林奇暗线（寻人启事/石蜡油手写标签/赵广成口述/库房黑皮针筒丧尸，四处不点破）；NPC 赵广成最佳，四个活人都在要食物、索取物同质。
- **待确认**：死亡后回溯按钮可见性（engine.js:769 显示逻辑被注释，1303 restart=clearSave 清档）→ 决定陷阱死亡惩罚是"撤销"还是"重开"，两种都不理想。
- 数值类（QTE 秒数/闪色次数/回满点/方姐时间窗文案）**未动，留给波波拍板**。

## 玩家报告整改 4 项（波波点名，WorkBuddy）
- 波波定性：**初始小区是故意整人的段落**（与作者博弈 + 收集死法），可随时撤回 → 那 4 处 P0 不改；记忆闪色 3色×7 不算难，真正有压力的是仁济南院正门×9；体力不是没压力——节点量够探索好几天，乱吃第二天就断粮。**别再按"难度过高"去改初始小区。**
- ① 文具店「仔细翻找」可拿饼干：新增 `anshengStationeryLoot(vars)`（安盛街.js 顶部，返回 [{key,name}]，三件各自独立判定），「都拿走」改为**函数式 showCondition/condition/effect**，格数按实际缺几件算（不再写死 +2）。文案三分支：饼干+传单都在 / 只剩传单 / 只剩饼干 —— 避免"文案承诺饼干但 effect 不给"。⚠ 饼干由此有了第 2 个来源，食品店 `dd<=2 && !hasBiscuit` 的窗口不再唯一，稀缺性下降（平衡，波波已知）。
- ② 全家「查看日记本」：删 `Math.random()<0.5`，改 `showCondition:"hasDiary"` → 100%（这是唯一教"打腿"的入口，原本 50% 随机直接卡死新手）。
- ③ 民防等候区隐藏计数器：文本改函数，按 `visitWaitingRoomTimes` 递进：0 无声 / 1 橙「门外似乎传来了什么声音…也许是听错」/ 2 橙「鞋底蹭过水泥地…这地方本来不该有别人」/ 3 红「刮擦声…不能再待下去」。**1 次可撤、2 次警告且"回头离开"仍可见（<=2）、3 次提示与"下一次操作即破门"对齐**。
- ④ 地铁站 QTE 拉长：站厅 8s→12s、安检 6s→9s、楼梯 4s→7s、站台 3s→6s（斜率不变；尸潮 5 级下限 9.5/6.5/5/4.5s）。
- 新增自检 `tools/playtest_fix_selftest.js`（**用 tools/story_files.js 加载，不硬编码清单**），45/45 通过；含 200 次渲染验证日记本 100%、满包走 elseScene、函数式 condition 与引擎 checkCondition 同构。
- 回归：`playtest_fix` 45/0、`condition_audit` 0/0、`scene_fn_selftest` 0 异常、`sprint_away` 0 P0、`bag_volume` 32/0、`noodle_stack` 64/0、`bottle_pickup` 72/0、`save_compat` 27/0、`scene_id_dup` 0、`e2e_smoke` 页面异常 0。lint E=1 W=113 全是既有 `images/建平/灵海社活动室.webp` 404，与本次无关。
- **体力遥测首批真实数据已分析**（输出 `tools/体力遥测分析-2026-09-26.md`）。109 条 delta / 净 −24.1。
  - **支出结构（按点数）**：饥饿 40.1%（23 次·每次 −1）｜连续移动疲劳 26.9%（10 次·均 −1.54，**低频高痛**）｜户外天气 24.3%（51 次·均 −0.27，**高频钝刀**）｜场景 add 7.0%｜夜晚 1.7%。
  - **已知收入仅 9.2 点 vs 支出 57.3 点 = 1:6.2** —— 存活本质靠「初始体力 7」和重开兜底，食物/休息存在感极低（休息全程只用 4 次 +4）。
  - **⚠ 日志跨 ≥4 个构建**：遥测日志持久化在 localStorage 且不清空，而 core.js 三天漂行号 4 次 → 同一个 `travel-fatigue` 赋值被记成 **core.js:381/388/389/415** 四个来源；饥饿规则同理被记成 engine.js:644 与 653（09-24→09-25 挪 9 行）。**跨天数据按来源直接比较无效，必须 `__clearStaminaLog()` 后重测。**
  - **归因语义实测固化**（`tools/stamina_src_attribution_probe.js`，真实源码 + 真实遥测段在 Node 里跑）：**对象式规则 → 归因到「应用侧」engine 行号；函数式规则 → 归因到真实赋值行（非定义行）**。
  - 危险信号：① 3 次体力被扣成负数再由 cap 钳回 0（对象式 add 无 Math.max 保护），紧贴 `strength<=0.01 → 结局-体力耗尽`（core.js:554）；② 未知来源 `'?'` 恰好 +8 × 3，已排除回溯/refreshComputed/Object.assign(flags)，疑似控制台或 native 帧，下轮用 raw 帧定性。
- **遥测加固（engine.js 遥测段）**：每条记录加 `lid`（页面加载 id）、会话事件加 `ver`（storyData 形状指纹 = 变量数-规则数-场景键数）、未知来源时附 `raw` 原始栈帧首行；`stamina_report.py` 总览新增「跨构建/跨加载」告警行。
- 回归：`stamina_report --selftest` 通过、`--list-anchors` 15/15、`stamina_telemetry_selftest` 9/9、`node --check engine.js` OK。

## 休息节点 × 整理整理入口（20 处）
- 需求：休息时能就地整理背包。**休息节点判定按代码不按名字** = `onEnter` 调 `restRecover(vars,n)` 的场景，全库 20 个（名字有"休息/歇脚/小憩/听音乐/窗边"五种叫法）。
- 新增通用工厂（`utils.js`）：`restTidyChoice(id)`（选项，`showCondition:"itemCount>0"` + 🎒 + `positionAfterOperation` 指回本场景）+ `restTidyGuard(vars)`（onEnter 开头短路）；新变量 `_restTidyReturn:false`（布尔，无需 `_caps`，未 bump SAVE_VERSION）。
- **⚠ 加入口本身引入 3 类刷子漏洞，必须配 guard**：`整理整理` 出口是 `{positionAfterOperation}` → 回休息节点 **onEnter 会再跑一遍** → ① 13 个节点重复推进 5~30 分钟；② 味千拉面两处重复 `chasedByZombies -1`（尸潮刷到 0）；③ 益丰-匀口饭吃重复 `_zhaoGuangchengFoodGiven++`（绕过"最多匀 2 次"门控且白扣）；④ **理发店-休息 夜间分支重复 `dd++` 跳天**。guard 一律放在 onEnter 最前（在计数/过夜分支之前）。
- 新工具：`tools/rest_tidy_audit.js`（枚举+待补清单+既有入口风险扫描）、`tools/rest_tidy_selftest.js`（**155/0**，S5 有 13 条 A 类"不守卫必重复推进时间"对照组防假绿）。报告 `tools/休息节点整理入口改造报告.md`。
- 回归全绿；**lint 本轮 E=0 W=113**（此前记的 E=1 `灵海社活动室.webp` 已消失，图片已补）。
- **⚠ 环境坑：U 盘工作区会脏读**。`git stash pop` 后立刻跑脚本读到未落盘的中间态 → `node --check` 报**实际不存在**的 `},,` 语法错、`condition_audit` 打印 `执行失败 story/建平中学.js` 却仍输出"0 处"（**假绿**，整章 240 场景没加载）、lint 报其实存在的图片 404。**判据：审计输出里出现「执行失败 <文件>」一律当失败，别只看末尾的 0**；大批量写入后先 `node --check` 全量 story 再跑回归。
- 遗留（未动，待波波定）：既有非休息整理入口 60 个中 4 个有 onEnter 副作用，只有 `全家便利店-员工通道`（`updateTime(3)`）是真的会重复 +3 分钟，其余 3 处是幂等 set 无害。

## 武器系统梳理（只读盘点，未改代码）
- 产出 `tools/武器系统梳理.md`。核心结论：武器分三套判定（近战 `meleeWeaponName`/`meleeWeaponTier`、重工具 `heavyWeaponName`、割锯 `cuttingToolName`），耐久走"方案C"——无耐久条，只有断/不断，`breakWeaponByName` 是唯一损坏入口。
- 实测挂点数：`tryBreakWeapon` 30、`combatDrain` 31、`weaponBrokeText` 53、`combatDrainText` 35、`useHeavyTool` 8、`countHeavyUse` 10、`hasMeleeWeapon` 引用 47、`meleeWeaponTier` 14。
- 数值（utils.js）：战损概率 弱50%/中25%/强10%（只挂失败节点）；体力 `combatCost` = tier≥2 ? 1 : 2（空手按弱档 −2）；撬砸寿命 铁管3/拐杖3/拖把杆1，**斧头无限不计**。
- 手枪体系独立：不进 `hasMeleeWeapon`/tier，耗弹不耗体力，首取 3 发由 `_policeGunTaken` 一次性锁死，丢枪清 `gunAmmo`，开枪一律 `chasedByZombies+2`。
- **已修复**：① 拐杖/拖把杆拾取点缺"已持有"闸门（安盛街:236/551）→ 加 `showCondition: "!hasCane"` / `"!hasMopHandle"`，与全库斧头/匕首/枪/美工刀拾取点一致，防未来可重入路径重复 +1 格。回归 condition_audit 0/0、scene_fn_selftest 0 异常、scene_id_dup 0、全量 `node --check` OK。
- **观察项（仍待定）**：② 斧头 3 个获取点（警察局/新达汇B1/建平车库）各自有 `!hasAxe`，不重复计数；但斧头撬砸无限 + 战损仅 10%，实质是最优解。③ 撬砸文案必须读 `vars._pryTool`，断后 `heavyWeaponName` 会指向次优武器。

## 物品守卫（item guard）缺陷扫描（只读查找，未改剧情代码）
- 波波提出：老吴杂物室-铁柜玩家没开柜、只因身上有螺丝刀（可能来自别处）就被文案告知"你已经有螺丝刀了"= 未解锁容器剧透。要我全库找这类"物品守卫"问题。
- 新增 `tools/item_guard_leak_scan.js`：全库源码扫描两类缺陷——A 剧透型（hasX 守卫下未开容器点名物品）、B 吞内容型（入口被 `!hasX` 包死致场景永久不可达）；依赖 `tools/story_files.js` 加载清单，不硬编码。复跑：`node tools/item_guard_leak_scan.js --md`。
- 结论（报告 `tools/物品守卫审计报告.md`）：A 类真 P0 三处＝建平中学.js:1664 铁柜 / :3418 物理实验室(5F) / :1017 地下车库-消防柜（都是"锁着的容器未开即点名/穿帮"柜内物）。B 类真缺陷 1 处＝东明街道/金谊广场.js:1342 金谊广场-B2-搜刮（已持防毒面具反进不去，丢失急救箱）。其余 B 类多为"重复拿取提示被合理吞掉"、A 类 P1/P2 经核为真 OK（物品场景内可见/本处来源）。
- **脚本判定坑（已修，记以防复发）**：场景块"已露面"判定曾误把①场景ID含物品词（"…-螺丝刀"）②同一条 `if(hasX) return "…物品…"` 守卫串③`nextScene:"…-螺丝刀"` 跳转串 当作玩家可见文案 → 把后两条真 P0 错判 OK。现排除"守卫分支区间 / 场景定义行 / 跳转与函数行 / 引用了被守卫变量的 `if` 行"，仅认"不依赖守卫的默认描述里出现物品名"。
- 波波只要求查找，未改剧情代码；修复方案待拍板（剧透句删或改中性描述——铁柜/消防柜需否新增 `_xxxOpened` 标记；B2-搜刮改为有面具也给"戴面具搜索"入口）。

## 物品守卫修复（波波拍板：3 处剧透句删 / B2 不改）
- 实施：建平中学.js 三处剧透句直接删除——铁柜 `if(hasScrewdriver) return "你已经有螺丝刀了。"`（原1664）、5F物理实验室 `if(hasScrewdriver) desc+="（你已经有一把螺丝刀了。）"`（原3418）、地下车库-消防柜 `if(hasAxe) return "…斧头你身上已经有一把了。"`（原1017）。删后均回落到中性描述；消防柜 hasAxe 分支本就不可达（拿斧后入口 `!_garageFireCabinet||!hasAxe` 为 false），属清死代码式剧透。
- **B2-搜刮=B2 报告误报，已撤销**：原报告把金谊广场-B2 地下车库 `choices` 的 `if(!hasGasMask||maskRemainingUses<=0) return [死亡]; else return [戴面具搜索车库,离开]` 反向误读——"戴面具搜索车库"其实位于 **hasGasMask 为真**分支（有面具才给搜索），正是预期。**扫描脚本 B 类启发式误把死亡分支的 `!hasGasMask` 当成兄弟 return 搜索入口的守卫**。佐证：每个面具来源都初始化 `maskRemainingUses`（建平/警察局=2，安居苑/樱桃苑/默认=1），先拿面具再来 B2 反而能进，不丢急救箱。故 B2 保持现状不改。
- 回归：`item_guard_leak_scan.js --md` 复跑 A 类 **P0=0**（原3），P1=3/P2=3 均为假阳性、B 类多来源=6 均非缺陷。报告 `tools/物品守卫审计报告.md` 已加「修复状态」节。

## 日记本系统实现方案（脑暴→方案，未写代码）
- 波波选定日记本系统（上轮脑暴点子④）。方案落在 `设计稿/日记本系统实现方案.md`，先方案后编码。
- **核心设计：engine.js 零改动**。日记本=普通场景，全踩现有机制：记忆 Set（判重）、input 选项（手写）、整理整理子场景（入口，同「看看时间」先例）、场景 text 函数+style（楷体渲染+分页）。
- 数据模型：`_diaryLog`（数组，条目 {kind: personal/game/mixed/event/note, key|text, dd,hh,mm,weather}）+ `_diaryPage`（分页）。日期/天气自动记录 → 渲染成「6月30日 · 星期二 · 雨」日期头。
- 新 helper `gainMemory(vars,key,type)`（统一记忆入口，幂等）改造 9 处裸 `.add()` 调用点；张江「检测中心-抄录」onEnter 现为空，接 `addDiaryEvent` 让"抄进日记本"文案落地成真条目。
- ⚠ 手写必须 HTML 转义（engine 用 innerHTML 渲染）；翻页自跳用 `_lastScene` 守卫防 onEnter 重置 `_diaryPage`；老档 fillMissingDefaults 补新变量、不 bump SAVE_VERSION。
- **待拍板 5 项**（见方案 §10）：拿本子前的记忆是否补记（推荐 A 一直记）/ 丢本子是否清 log（推荐不清，且"丢下日记本"现无预警裸丢，建议加提示）/ 写日记耗时 0 or 1 分钟 / 单条 maxLength 80 / 每页 5 条。
- 验收计划：新 `tools/check_diary.js`（memorySet 裸 add=0 + DIARY_ENTRIES 双向对齐）+ 三件套 + save_compat。跨周目（独立 localStorage key、run 字段分墨色）留 Phase 2，内部重开实装后再做。

### 日记本系统实施（波波拍板全按推荐：①A补记 ②A丢本特殊节点+log保留 ③1分钟 ④80字 ⑤每页5条）
- **engine.js 零改动落地**。`story/日记本.js`（DIARY_ENTRIES 11 条 + diaryRender 分页 + 日记本/日记本-写/写-空白/写好了/整理整理-丢日记本 5 场景）；utils.js 加 `diaryEscape/gainMemory/addDiaryEvent/addDiaryNote/diaryDateText`（日期头 Day1=2026/6/29 由 Date 换算）；core.js 注册 `_diaryLog/_diaryPage` + 整理整理加「📔 翻开日记本」+ 丢日记本改跳特殊节点（波波亲写文案"地上已经空空如也，像是未曾有过什么"）。
- **11 处调用点改造**：9 处 personal + 张江抄录接 `addDiaryEvent("抄录报告")` + **建平「腐烂尸城」mixed**（方案初稿漏 mixed，最初 grep pattern 只写了 personal|game——**教训：记忆类 grep 必须含 mixedMemorySet**）。
- **踩坑 2 个（审计抓出）**：①DIARY_ENTRIES 值写成纯字符串但 diaryRender 按 `{body}` 取 → `ent.body` 恒 undefined、正文全空（渲染只剩日期头）；已改为 diaryRender 兼容字符串与 `{body,ink}` 两种。②手写转义必须含花括号中和（`{`→`&#123;`）——text 函数返回值会做 {变量名} 插值，玩家写"{dd}"会被替换成天数；HTML 转义防 innerHTML 注入。
- 设计：同一天条目共享日期头（天气取落账快照）；翻页自跳 onEnter 靠 `_lastScene !== "日记本"` 判外部进入才归位最新页；写好了 onEnter=updateTime(1)；空本子/越界页码 clamp 兜底。
- 回归全绿：**check_diary 35/0**、condition_audit 0/0、scene_fn 0 异常、sprint_away P0=0、save_compat 27/0（⚠1 bag 为已知旧档缺陷）、scene_id_dup 0、bag 72/0、bottle 72/0、noodle 64/0、playtest 45/0、rest_tidy 155/0、jp_bell 23/0、bridge 22/0、xin 74/0、unarmed 34/0、mercury 37/0 + e2e 20/0；全量 node --check 26 文件；ai_phrase_scan 日记正文零命中、"安静"基线 113 无漂移。tools/story_files.js FALLBACK 已同步 +1。
- 遗留：纸面图 16:9 待波波生成（现 placeholder）；跨周目存档（Phase 2）等内部重开机制。

## 日记本跨周目账本（ledger）
- 需求：两条重开路径（engine 右上角"重新开始"按钮 / 刷新页存档框"从 Day 1 重新开始"）都保留日记本信息；有日记本则记忆数据不丢。
- **核心设计决策（依据核心设定 §2.2）**：记忆 Set **不回灌**新周目（回灌 = A/B 拦截提前 = F 永远不可达）；跨周目累积数据 = `_diaryLog` 本身 + ledger 留档的 Set 快照（备查）。
- 实现：`utils.js` 加 `persistDiaryLedger`（清档前落账，仅 `hasDiary` 才有继承资格；独立 key `shichaobiji_diary_ledger_v1`）/ `restoreDiaryLedger`（樱桃苑「拿上日记本」effect 调用；sig 去重保本周目页、按 run+时刻排序旧账在前、条目补 `run` 戳、`_runNumber = ledger.runs+1`）/ `diaryEntrySig`/`diaryEntryOrder`。engine.js 仅两处一行钩子（typeof 守卫，Node 审计可加载）+ makeSaveSummary 显示「第 N 周目」。
- 渲染：周目交界分隔行「字迹换了墨色」+ 旧周目条目褪色 span（#bfb7a3）。坑：prevRun 哨兵 0 会让本子第一页误出分隔行——首条时 prevRun 应取首条目 run。
- 丢弃日记本 = hasDiary=false → 不落账 = 数据随本子消失（对上"空空如也"文案）。
- 审计：check_diary.js 加第 7 节（engine 钩子源码断言 + mock localStorage 往返测试），53/0。**测试自身踩坑**：persist 按 hasDiary 拒绝落账，二周目落账断言前必须先补置 hasDiary=true。
- **顺手修既有失败**：noodle_stack_selftest 期望旧动态文案「拿一包泡面（货架上还剩 N 包）」，剧情已改为固定文案（货架余量挪进「全家-拿泡面」正文）→ 测试三处 click/has 期望改平，64/0 恢复。经 git stash 验证为 HEAD 既有失败，非本次引入。
- 回归全绿：check_diary 53/0、condition_audit 0/0、scene_fn 0 异常、sprint_away P0=0、save_compat 27/0、rest_tidy 155/0、bag 72/0、bottle 72/0、noodle 64/0、playtest 45/0、全量语法 27 文件。
- 遗留：结局落日记（"已触发的结局新增一页"）、扉页隐藏文字（跨周目阈值）、催更后"内部重开等价外部重开不保留"的逻辑、F 结局判定读 ledger —— 都等结局节点系统实装。

## 钥匙类道具统一（占格 + 可丢弃）
- 波波拍板：10 把持久钥匙类道具**全部占 1 格背包、全部可丢弃**（含原不占格的仁济门禁卡 `hasRenjiCard`、员工通道钥匙串 `_hasCampusKey`；原不可丢弃的黄铜门钥匙2 `hasDoorKey2`、金宝贝钥匙牌门钥匙3 `hasDoorKey3`、仁济门禁卡、老吴钥匙串 `hasKeyRing`、员工通道钥匙串）。
- 改动：① 安居苑.js:763 门禁卡 onEnter 补 `add:{itemCount:1}`；② 上实南校.js:139 员工通道钥匙串 onEnter 补 `add:{itemCount:1}`（与 hasKeyRing 击杀获取无条件 +1 同口径；背包满时临时越界 1，丢一件即恢复，非软锁）；③ core.js 整理整理新增 5 条「丢下」入口（黄铜钥匙/金宝贝钥匙牌/仁济门禁卡/老吴钥匙串/员工通道钥匙串）；④ 修正原"钥匙串不设丢弃"注释（管线图仍不设丢弃）。
- 分析/落档：`tools/key_inventory_analysis.md`（含统一前后对照）。
- 回归全绿：condition_audit 0/0、scene_fn_selftest 0 异常、bag_volume_selftest 32/0（1 项旧档迁移已知缺陷非本次）、save_compat_selftest 27/27。
