# 2026-09-22 工作日志

## 排查：控制台改 `gameState.weather` 为何会被"还原"
- 用 `tools/test_helper.mjs`（Playwright E2E）写临时探针实测，结论已确认，探针脚本已删。
- **写入本身是生效的**：`weather` 是 core.js `_variables` 里的普通变量，同小时内连续走 20 步都保持 `雨`。
- **真正的改写方只有一个：`utils.js:86 updateWeather(vars)`**，由 `utils.js:29 updateTime` 在「跨整点 `hh` 变化」**或**「单步 `mins >= 60`」时调用。实测 8:42→9:08 一步就触发，`雨 → 阴`（50%）。
- 概率表（实测吻合）：雨→50% 阴；阴→(6-11点)30% 晴 /(12-17点)35% 雨；晴→(12-17点)30% 雨 /20% 阴。**雨的唯一出口是阴**，所以特别像"变回原来的天气"。
- 全库时间步长分布：1min×1200、2min×408、3min×107、5min×77、10min×31、30min×22、15min×15、180/120 各 1 —— 道路长段多，跨整点常见。
- **第二个坑（视觉）**：`engine.js:1148` 每个场景无条件把 `showRain=false`，且雨天 CSS 类条件为 `weather=="雨" && showRain`（core.js:519）→ 即使 weather 还是雨，走出下一步雨滴动画也会消失。
- 附带：户外场景 `engine.js:1160` 自动 `applyWeatherDrain()` → 扣体力 / `_rainExposure +20`，测试时会看到意外掉体力。
- 测试雨天建议：`gameState.weather='雨'; gameState.showRain=true; applyScreenEffects();`，并注意回溯/读档后 `gameState` 会换成新 Proxy 对象，挂在其上的 defineProperty 锁会失效。

## 过夜系统：建筑类过夜点加「去过才出现」门槛
- 新增工具 `node tools/overnight_shelter_audit.js`（可回归）+ 报告 `tools/过夜系统审计报告.md`。
- **`天黑必须过夜` 共 29 个选项**，改动前只有 5 个带 `_visit` 门槛、1 个靠 condition 事后判定、23 个完全无门槛。
- 定下的口径：**两层门槛**。① `showCondition` 加 `_visit['建筑内部场景'] > 0`（没去过=选项不出现）；② `condition`+`elseScene` 原样保留（去过但没清场/没钥匙=仍会死）。就地过夜（`currentPos` 已在店内）与区域兜底不加第①层。
- 本次加门槛 8 处：理发店 `'理发店内部'`、全家 `'全家便利店（环林东路）'`、安居苑 `'三林安居苑-小区内部'`、图书馆 `'图书馆'`、深夜食堂 `'新达汇-哥哥的深夜食堂'`、上科大 `'张江-上科大-校门'`、金谊2F `'金谊广场-2F-休息'`、联华超市（修键名）。
- **深夜食堂 cond 改 `_yorozuyaUnlocked || hasDoorKey1`**：否则店里选「锁好门」会把 `_yorozuyaUnlocked` 置 false，带钥匙的玩家夜里被判定进不去。
- **悬空 `_visit` 键修 4 个**：`'小超市'`→`'联华超市'`（此前过夜选项永不出现，`过夜-小超市`/`结局-过夜-小超市暴露` 长期不可达）、`'4F电梯厅'`→`'新达汇-4F南走廊东'`、`'全家-喝饮料腹泻'`→`'结局-全家-喝饮料腹泻'`、`'金谊广场-B1奥乐齐-搜刮'`→`'...-搜刮-吃完'`。
- **4F 那处的教训**：该键用于 `新达汇-4F南走廊东`，判据 `==1 / >1`。**改名前必须算首达路径**——从「中庭环廊→北走廊中→北走廊东→绕到南走廊东侧」进来时 `新达汇-4F电梯厅` 计数为 0，`==1`/`>1` 同时为假 → 0 选项 → 走 `engine.js:1004` 的「没有可行的选择……剧情终止」。全库同款计数写法都是**自引用本场景 ID**，且 `_visit` 在 `engine.js:1144` 先自增后渲染，故自引用语义成立。
- 审计脚本踩坑：`_visit` 键扫描要**跳过 `//` 注释行**，否则注释里写的示例键会被误报成悬空。`nextScene` 校验要忽略 `{positionAfterOperation}` / `{_sprintDest}` 这类占位符。
- 回归全绿：condition_audit 0/0、overnight_shelter_audit 悬空 0/失效跳转 0、bag 33/0、unarmed 34/0、stamina_telemetry 9/0、stamina_audit ±130 平衡、stamina_report 自测通过。
- 遗留：`story/尸潮安全屋.md` 仍 v2 过期（已加顶部提示）；「9 个悬空场景 ID」待办现清掉 4 个。

## 东明街道 · 户外节点雨天表现覆盖审计
- 新增工具 `node tools/rain_image_audit.js` + 报告 `tools/户外节点雨天表现审计报告.md`。
- 规则：`outdoor: true` 节点三选一——① `image` 为 `images/placeholder.png`；② `onEnter` 开 `showRain`；③ 路径含「雨」的专属雨天图。
- **口径细节**：`showRain` 每场景由 `engine.js:1148` 无条件清零，只能由**本场景自己的 `onEnter`** 开（对象式 `{set:{showRain:true}}`、函数式直接写 `vars.showRain=true`、或 `travelScene`/`hideOnLocation` 的 options）。**选项 effect 里开无效**。雨天 CSS 类条件 `weather=="雨" && showRain`（core.js:519）。
- 结果：东明街道 34 个户外节点，32 合规（占位图 4 / showRain 11 / 雨天图 17），**2 待处理**；夜晚剧情+上海市区路径 11 个全合规。
- **待处理 ①** `全家便利店（环林东路）`（全家和公交站.js:4）：户外，无雨天图、无 showRain，雨分支只回了 `-night.webp`（夜间图）。该店门口**没有下雨天美术**，最省修法＝加 `onEnter:{set:{showRain:true}}`。
- **待处理 ②（真 bug）** `金谊广场地面入口`（金谊广场.js:463）：image 函数 `var f=timeImage({...}); return f;` **漏了 `(vars)`**，返回了工厂函数本身 → `sceneImage.src` 变成函数源码 → **晴阴雨 12 组合全坏图**。而雨天图 `images/金谊广场/正门-雨.webp`/`-雨-night.webp` **已存在却永远取不到**。
- **顺带同款 bug**：`金谊广场-正门硬闯`（金谊广场.js:520，非 outdoor 但同样坏图）——同文件 `return f;` 漏调用共 4 处：**行 471/477/527/533**，统一改 `return f(vars);`。
- 全库（含建平/仁济/张江）复核：除金谊广场这 2 个节点外，**再无**「image 返回非字符串」的坏图节点。用 `= timeImage(` 后接 `return X;`（非 `X(vars)`）的 grep 找漏调用，但**必须动态跑一遍**才准（`建平中学.js:21 return f;` 是数字楼层，不是坏图，属假阳性）。
- 脚本实现：vm 沙箱加载 `utils/core/夜晚剧情 + 东明街道/*.js + 上海市区路径`（**不载**建平/仁济/张江）；按 3 天气 × 4 时段（9/17/20/1 点）采样 `scene.image`，覆盖 `timeImage` 的 morning/evening/night/midnight 分支；用「加载前后 keys 差集」归因场景来源文件。

## 水瓶（hasBottle）剧情提及全库审计 + 可拾取化建议
- 新增工具 `node tools/bottle_scan.js` → 报告 `tools/水瓶剧情提及审计报告.md`；人工精筛报告 `tools/水瓶可拾取化改造建议.md`。
- **扫描口径踩坑**：裸 `/瓶/` 会把电瓶车/酒瓶/药瓶/盐酸瓶/油漆罐全捞进来（123 场景）；收紧到「饮用水容器」语义后 = `水瓶|矿泉水|瓶装水|空水瓶|空瓶|塑料瓶|桶装水|纯净水|凉白开|脉动|饮料瓶` → 74 场景。**另注意补 `脉动`**，仁济南院特需病房的关键道具就是脉动，容易漏。
- **双轨法（推荐复用）**：轨1 = 全库扫 `hasBottle\s*[:=]\s*true` 的**权威拾取点**（与措辞无关）；轨2 = 关键词逐行普查。只靠轨2 会漏——`金谊广场-停车场-搜刮` 写的是"半瓶矿泉水"，不含"水瓶/空瓶"，但它是真拾取点。
- **场景切块正则**：`/^  "([^"]+)"\s*:\s*\{/gm`（story 文件顶层 2 空格缩进的 `Object.assign(storyData,{...})` 键），实测可靠；行号 = `src.slice(0, mk.at).split('\n').length`。
- **审计结论**：全库 74 个含瓶场景，真实拾取点**只有 5 个**——`联华超市-仓库-拿水`(带水)、`金谊广场-停车场-搜刮`(带水)、`金谊广场-4F`(空瓶)、`三林安居苑-8号楼-204室-空水瓶`(空瓶+老洪「芜湖 6.25」标签瓶)、`张江-华大-动力站-纯水`(空瓶，UPW-QC 取样瓶)。其余 53 个为纯环境叙事。
- **⚠ 关键平衡发现**：`上实南校-图书馆-给水`（上实南校.js:874）**每次吃掉一整个瓶子**（`hasBottle=false`+`itemCount-1`），给满 5 次才给轿车钥匙；`益丰大药房-喂水`（:581）也吃掉整瓶。全库 5 个来源 ≈ 5 次给水，**水瓶是紧平衡资源，任何加瓶都直接降这条长线的难度**。建议加瓶集中在与上实南校动线不重叠的中后期图（仁济南院/张江/建平），东明街道早期动线（银行-拿水、安盛街-文具店铁柜-吃喝）先不动。
- **最值钱的候选**：① `仁济南院-特需病房-病床`（:1947，脉动"瓶子还凉"，现在只能当场喝掉，加第三选项即可给「带水瓶」，且仁济全图无水瓶来源）；② `新达汇-电梯厅贩卖机-翻找`（:1082，文本明写落货口有空矿泉水瓶却只给旧钞票）；③ `建平-挹芬楼-3F-高一教室`（:3234，"桌缝里夹着一只空水瓶"，能激活 `建平-弘渊楼-2F` 的「保温杯水灌进水瓶」支线——但该校保温杯水**全是甲基汞毒水**，等于放大陷阱，需拍板）；④ `新达汇-1F后勤仓库`（:3750，文本摆好了几箱矿泉水，场景却零交互）。
- **明确不要动**：`地铁站-站厅层-声东击西`（一次性投掷道具）、`上实南校-图书馆-全灭`（"就是你上次给她的那一只"情感 callback）、`新达汇-B1后勤走廊/-配电房门外/-配电房-内部`（刘志鹏线索链，"瓶子朝门"是坐标暗示）、`张江-上科大-曹睿泽宿舍`（桶装水桶=推理证据链）、`金谊广场-童涵春堂`(Hg药瓶)、`新达汇-1F数码店`（高锦睿道具）、益丰赵广成递水（人情+休息机制）。
- **两层语义别混**："水=食物"（成箱成提矿泉水，已由 `supermarketWaterLeft`/`restRecover` 表达）与"瓶=工具"（容器）是两回事，不要把前者的矿泉水做成水瓶。
- **拾取四件套**（照抄现有 5 处）：`condition:"itemCount < bagVolume"` + `effect:{set:{hasBottle:true,bottleWater:0|1,waterToxic:false,_hongBottleLabel:false},add:{itemCount:1}}` + `elseScene:"整理整理"`。丢弃/送人时必须同步清 `bottleWater`/`waterToxic`/`_hongBottleLabel` 三个附属变量。
- 附：保温杯是另一套容器体系（`建平-弘渊楼-2F` 已有「喝水/灌瓶」完整机制、`全家便利店-员工通道-摸黑` 当诱饵扔出去、其余纯叙事），未纳入 `hasBottle`。

## 水瓶可拾取化改造 · 4 处落地（承接上节）
- **作者确认平衡不成问题**：联华超市 + 长者食堂（有饮水机）**可多次打水**，水瓶不是紧平衡资源；真实难度来自来回赶路的体力消耗 → 原先"5 来源 ≈ 5 次给水"的紧平衡顾虑作废，4 处全部加。
- **新增 2 个计数变量**（`core.js` `_variables`）：`vendingBottleLeft: 3`（新达汇1F电梯厅贩卖机落货口空瓶上限）、`newdahuiWarehouseWaterLeft: 20`（新达汇1F后勤仓库未开封箱装水，2箱×10）。**插了 2 行 → core.js 下方行号 +2**，`tools/stamina_report.py` 的 `KNOWN_SITES` 6 个 core.js 区间与 selftest mock 行号已同步校正。
- **新增辅助函数** `cuttingToolName(vars)`（`story/utils.js`，优先级 美工刀 > 匕首 > 斧头 > 螺丝刀，无则空串）。**故意做成普通函数而非 computed 变量**——加 computed 会又给 `condition_audit.js` 加一批假阳性。使用门槛写法：`condition: function(v){ return !!cuttingToolName(v); }`。
- **4 处改造**：
  1. `新达汇-电梯厅贩卖机-翻找`（新达汇.js:1059/1093）改成可反复进入；新增 `新达汇-电梯厅贩卖机-捞空瓶`（:1106）每次 `vendingBottleLeft--`，给空瓶。捞空后「把手伸进豁口翻一翻」走 `-空手` 并提示"捞干净了"。
  2. `新达汇-1F后勤仓库`（:3792）**两层设计**：有割/锯工具 → 「用美工刀划开纸箱」→ `-开箱`（updateTime(5)）→ 可反复 `-拿水`（密封满瓶，`waterLeft--`，占背包格）或 `-换水`（已有瓶则只换水不占格）；无工具 → 「试着徒手撕开纸箱」→ `-撕不开`（提示需要美工刀）。箱外那瓶走 `-已开封瓶`（:3895），徒手可取，**不消耗箱存**。净效果：有工具=水+容器，无工具=只有容器，保留区分度。
  3. `建平-挹芬楼-3F-高一教室`（建平中学.js:3234）文本按 `!hasBottle` 动态显示"桌缝里还夹着一只空水瓶"，新增 `-空水瓶` 节点（:3271）。**副作用已确认**：会激活 `建平-弘渊楼-2F` 的「保温杯水灌进水瓶」支线，而该校保温杯水**全是甲基汞毒水** → 等于给陷阱多一条入口（作者已知悉）。
  4. `仁济南院-特需病房-病床`（仁济南院.js:1962）加第三选项「连瓶一起收进背包」→ `仁济南院-特需病房-病床-带走`（:1972）给**满瓶脉动**；原「当场喝掉」→ `-功能饮料` 与「放回去」保留。`-病床-空` 文案区分"喝掉"与"带走"。
- **所有新节点都 set `positionAfterOperation = 本场景ID`**，避免背包满走 `整理整理` 时掉进空场景。
- **顺带修掉既有 bug**：`图书馆-阅览室`（东明社区图书馆.js:188）ASI 坑——`return` 换行接字符串 → 首次进入正文返回 `undefined`（空白）。
- **新增 2 个回归工具**：
  - `tools/scene_fn_selftest.js` —— 补 `condition_audit.js` 的**函数式条件盲区**（后者只查字符串条件，函数式整段跳过＝假绿）。vm 沙箱载全库 1406 场景 × 7 状态变体实调 text/choices/condition/onEnter/跳转，抓异常、text undefined、跳转目标不存在。现 **0 异常**。
  - `tools/bottle_pickup_selftest.js` —— 迷你引擎复刻 `renderChoices`/`checkCondition`/`applyEffect`/`parseRedirectTarget`，72 断言覆盖贩卖机 3 上限、仓库工具分支与 20 上限、建平空瓶、仁济满瓶。现 **72 通过 / 0 失败**。
- 回归全绿：condition_audit 0/0、scene_fn_selftest 0 异常、bottle_pickup 72/0、bag 33/0、unarmed 34/0、overnight 悬空0/失效0、stamina_audit Σ+130/Σ-130、stamina_report selftest 通过、stamina_telemetry 9/0。
- 文档：`tools/水瓶可拾取化改造建议.md` 加 §0 实施记录，§1/§2 标记"已全部实施"，§6.1 记录顺带发现的既有问题。
- **遗留待作者拍板**：`联华超市-仓库-拿水` 拿瓶时**不加 `itemCount`** → 丢弃后会多出一个背包格（属平衡决策，已记录未改）。
