# 2026-09-18

## 丧尸气味一致性审查（剧情文本）

**任务**：排查剧情中所有「甜腻」气味表述，评估生物学合理性并给出改法。

**排查结果**：`story/` 下共 19 处「甜腻」，其中 14 处为气味描写（另 3 处是味觉：葡萄糖/爆米花/八宝粥，无需改）。分布：
- 仁济南院.js（太平间 3 处）
- 张江.js（华大工位C 2 处 + 川杨河/大桥尸群 4 处）
- 东明街道/金谊广场.js（B2 车库 + 货梯间——货梯间写的是「那股甜味」，易漏）
- 东明街道/樱桃苑（初始小区）.js（4 楼）
- 东明街道/安居苑.js（204 室 2 处 + 7号楼301 馊水丧尸）
- 东明街道/五金店.js（后巷女丧尸口气）

**核心结论（待用户定夺）**：
1. 「甜腻」的成因是**蛆虫代谢**，只在尸变 1 周以上出现；中期（2–7 天）的「甜」来自厌氧发酵产气（甜酸/捂坏果酒感），两者成因不同不可混用。
2. 建议把「甜腻」从"通用丧尸味默认值"降级为**晚期专属信号**：早期全部换成中期措辞（腐胺/尸胺 → 闷重腐臭 + 臭鸡蛋 + 组织的湿臭），只在张江川杨河/大桥尸群 + 仁济南院太平间保留。
3. 次级建议：气味与丧尸**类型**绑定——毒气型=发酵甜酸/化工甜腥，黑皮（高汞）=金属腥，普通型随时间由血腥→腐肉。

**顺带发现的疑似时间线问题**：
- 樱桃苑民防设施日记（`樱桃苑（初始小区）.js:1469`）写「7月14日（或者15日）… 被困第十四天」，但开局是 6/29，玩家第 1–2 天即可读到该日记 → 需确认是否为有意的时间线伏笔。

**已执行（用户选择「按建议全改」）**，共 15 处改动，7 个文件，全部通过 `node --check`：
- 早期改中期措辞：樱桃苑（初始小区）.js:1616、安居苑.js:591/947/1731、长者食堂.js:243、张江.js:1799/1826、五金店.js:227
- 化工甜改为明确的「甜腥/甜酸」：金谊广场.js:563/574/576/578/1153/1157、张江.js:1875/1877
- 晚期保留甜腻并补「点破」台词：仁济南院.js:1307（太平间加一句「这味道甜得不正常，是烂了太久、已经生蛆的尸体才有的」）、张江.js:2866（滨河绿道加一句「那甜味不对劲——东西要烂到生蛆，才会泛出这种甜」）
- **额外发现**：五金店本来就是「化工品 + 甜」的自洽体系（第 20/21/47/144 行），227 行「熟悉的」正是指这家店的味道 → 最终改成「熟悉的甜腥——化工品混着腐臭」，而非简单删甜。
- **新确立的项目约定**见 `.workbuddy/memory/MEMORY.md`（丧尸气味写作规范）。

## 建平中学.js 体验评审（跳转/逻辑/文本）

**任务**：以玩家视角通读 `story/建平中学.js`（约 3035 行），找出跳转错误、状态矛盾、文本唐突/AI 风处。图片资源不在范围内。

**交付**：`建平中学-体验反馈.md`（项目根目录，24 条问题 + 引擎层 3 条附注）+ 临时校验脚本 `tools/check_jianping.js`。

**用过的自动化手段（可复用）**：`tools/check_jianping.js` 在 Node `vm` 里按 index.html 顺序加载 utils.js/core.js/全部 story 文件，然后逐场景
①静态+函数型的 `nextScene/elseScene/timeoutScene/onTimeout` 目标存在性检查；②所有 `condition/showCondition` 字符串求值（专抓 `_variables` 未注册变量）；③列出无选项节点（剧终）与 text 为空场景。
本次结果：条件表达式 **0 报错**（建平的变量注册很完整）、跳转目标 **2 处不存在**（均指向 `建平-挹芬楼-1F`）。

**确认的严重 bug**：
1. `jpStairChoices` 保底分支 → 挹芬楼楼梯间生成不存在的 `建平-挹芬楼-1F`（1F 已拆成东西走廊），玩家点「去1楼」即卡死；触发条件很常见（从 1F 走廊进楼梯间 / 清场后 `nextScene: sceneId` 自跳）。同时保底会丢掉「只上下 1 层」限制。
2. `建平-食堂-后厨` 每次 +20 gasIndex，第 5 次达 100 → core.js 全局触发器 `gasIndex>=100 → 结局-煤气中毒` 在 onEnter 之后立即生效，**跳过 QTE 直接即死**，与「窒息节点不致死」的设计自相矛盾。
3. `jpBlockedStair` 枪选项：引擎是「先 effect 后 nextScene」，扣弹后判 `gunAmmo>0` → **正好剩 1 发时必死**。同款写法还在长者食堂/金谊广场/全家（非建平文件）。
4. `建平-致真楼-1F-老吴杂物室` 诈尸分支只判 `dd>=3 && !_laowuKilled`，未判是否已搜过尸体 → Day1-2 搜过尸、Day3 回来尸体还诈尸；且 `搜尸体/击杀/抢管线图` 三处都 +1 itemCount，会虚增。
5. `NON_PLACE` 正则漏 4 项（`空枪`/`锁着`/`没钥匙`/`听音乐`），其中 `建平-结局-空枪` 是结局节点被误记入 Harsh 轨迹。

**规范符合度**：9 处「🎒整理一下物品」全部缺 `showCondition: "itemCount > 0"`（其他区域均已带）；21 处对白用了 ASCII `\"`（其余 20+ story 文件合计仅 2 处），是全项目最集中的引号不规范处。

**顺带确认的引擎层隐患**：`story/utils.js` 的 `updateTime` 里 `addMinutes` 被闭包内改写（雨天 ×1.3 累乘），凡把 `updateTime(N)` 写进**静态 choices 数组**的场景（建平大量如此）在雨天会越走越慢——需改成闭包内局部变量。

**图片**：`建平-前门` 四个分支都引 `images/仁济南院/前门*.webp`（该目录无此文件），而真实资产在 `images/建平/前门*.jpg`；另全局 TODO 注释写的是 `images/jianping/`（英文），与实际中文目录 `images/建平/` 不一致。

**未改动任何剧情代码**（纯评审）。

## 仁济南院.js 体验评审（跳转/逻辑/文本）

**任务**：以玩家视角体验 `story/仁济南院.js`（2096 行），找跳转错误、剧情唐突/AI 风处。图片质量不在范围。

**交付**：`仁济南院-体验反馈.md`（项目根目录，17 条问题 + 机器校验附注）；新一次性校验脚本 `tools/check_renji.js` + 报告 `tools/check_renji_report.txt`。

**机器校验**：99 个仁济场景，跳转目标/条件变量/函数求值全部 0 错误——无死链，问题全在状态一致性与设计层。

**确认的最重要问题（均已核实到引擎实现）**：
1. **太平间 →「去检验科」无条件直达 `检验科-内部`**（L1347），绕过电子锁+守卫丧尸；且 `_renjiLabCleared` 仍 false，外门再演"里面有动静"，sequence break。
2. **王知筠手机视频可永久错过**：`拿起手机` showCondition `!hasWangPhone`，捡起即置 true，场景只能进一次；电量<6% 且无充电器（hasCharger 在图书馆）或选"收起手机"→ 真相核心视频永远看不了；`整理整理` 无"用手机/充电"入口。
3. **检验科后门 `outdoor: true`**（L444）是室内场景：引擎对 outdoor 每次进场景调 `applyWeatherDrain`（晴扣体力/雨积受凉/晴清零 chased）。
4. **门诊大门清场后文/按钮不更新**：`_renjiGateCleared` 后仍显示"硬闯门诊正门"（实际 elseScene 直进）、正文仍说"要先对付丧尸"；且闪色"失败"不置 cleared，与急诊/检验科/走廊/太平间"受伤也算清场"惯例相反。
5. **大门闪色 placeholder 教错答案**（L119 示例含黄/白，但序列池只有红蓝绿，`checkFlashAnswer` 精确计数比对，照抄必错）；9 连还是全图最长、放在第一个战斗。
6. **手机电量公式 `15-(dd-3)*3` 在 dd<3 溢出**（21%/18%>15%，充电反而降电量）。
7. **renji-siege 封顶 chased=4 = 去高架必死阈值**，仁济内无 sprintAway/躲藏降追逐手段，阴雨天困死循环（晴天户外自动清零是唯一暗门）。
8. 方瑜手机文本矛盾：L456"屏幕早就黑了" vs L504"还亮着最后一点微光"。
9. 枪击 +1 chased 而倒车撞门（更响）零代价——噪音经济学不一致。
10. 特需病房先搜储物柜=即死（伏笔只有"柜体沾血"），首周目全凭猜；储物柜-警觉文案提及未读过的线索（条件是"或"文案是"和"）。

**做对的部分**：真相链五视角分层、`_renjiNoise` 与夜晚检验科失守联动、气味规范执行到位（甜腻只在太平间）、电梯厅/停车场电梯口两部电梯映射核对无矛盾。

## 局部新奇设计脑暴（未实装，仅供选题）

**任务**：应要求 brainstorm 让游戏超出"纯文字剧情"的局部新奇设计。产出四条线共 20+ 个点子（对话内交付，未写文件），两个概念图（业主群聊天界面 / 日记本手写界面）。

**主推点子（按建议优先级）**：
1. **口渴值 `_thirst`**：把世界观核心（汞→极度口渴）机制化，被咬后累积、文本插值分档、高渴解锁"喝积水"危险选项；丧尸动机统一为"渴"（雨天张嘴淋雨、水源=尸群热点）。
2. **通勤尸潮**：丧尸保留生前习惯，11:30/18:00 整点成群"通勤"，纯 choices 按 `hh` 分流即可实现，性价比最高。
3. **业主群 + 手机伪 OS**：聊天气泡界面读小区群（物业通知藏保命信息/团长接龙/撤回消息的恐怖），依托现有 `hasPhone/phoneBattery` 做每日刷新容器。
4. **日记本可写字**：跨周目核心道具允许玩家输入"今天写点什么"并带到下周目，字迹颜色区分周目/作者（wqx2 可夹带字条），直通【反向创作】真结局伏笔。

**其他有沉淀价值的点子**：气味条 `_stench`（气味写作规范机制化，高气味吸引丧尸/涂腐泥伪装）、广场舞音响丧尸（声音预警）、白板节点=玩家输入剧情（input 存变量、真结局引用）、外部重开格式化演出（NPC 抗议一闪而过）、结局收集=设计师测试日志、NPC 记忆涟漪（日记 flag 门控）、记忆闪色地铁线路色变体、雾天截断文本/台风天气、假报错美学（白板节点渲染报错风格界面）、丧尸舔玻璃指纹 overlay、打字机打错字回退（wqx 专属）、自助收银机/安检员等"制度惯性 NPC"黑色幽默系列。

**落地评估要点**：纯数据可落地=通勤尸潮/业主群文本/吐槽背包/制度惯性 NPC；需 `_variables`+computed=口渴值/气味条；需 engine 小改=日记本输入跨周目/打字机打错字/雾天截断/指纹 overlay。用户尚未选定先做哪个。
