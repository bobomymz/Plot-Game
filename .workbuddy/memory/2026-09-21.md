# 2026-09-21

## 背包容量体系改造落地（用户定案：一个主背包 + 一个袋子）

**用户设计**：正常最多带「一个包 + 一个袋子」（再多没手对付丧尸）。默认小背包容量 3；包不分种类但分容量，可换大包；袋子（帆布袋）统一命名，持有即 +1。

**改动清单**：
- `core.js`
  - `bagVolume` 从普通变量改为 `_reactive.computed` 派生式：`3 + _bagTier + _bagExtra`（第 343 行）。剧情代码不再直接改 bagVolume。
  - 新增 4 个变量：`_bagTier: 0`（0默认/1双肩包/2书包）、`_bagExtra: 0`（袋子加成）、`hasBackpack`、`hasSchoolbag`。
  - 整理整理「丢下帆布包」→「丢下帆布袋」，副作用 `add: { bagVolume: -1 }` → `add: { _bagExtra: -1 }`。
- `安居苑.js`
  - 新增主背包：`三林安居苑-8号楼-203室-双肩包` 增加「背走这只双肩包」选项（闸门 `vars._bagTier < 1`）+ 新结果场景 `-背走双肩包`（`set: { hasBackpack: true, _bagTier: 1 }`）。
  - 卧室「拿上帆布包」→「拿上帆布袋」，`bagVolume:+1` → `_bagExtra:+1`。
- `建平中学.js`
  - 新增主背包：`建平-挹芬楼-3F-高一教室` 增加「从地上捡一只书包」选项（闸门 `vars._bagTier < 2`）+ 新结果场景 `-捡书包`（`set: { hasSchoolbag: true, _bagTier: 2 }`）。
- `新达汇.js`：`帆布包` → `帆布袋`（场景 ID `新达汇-2F杂物间-帆布包` → `-帆布袋`），副作用改 `_bagExtra`。
- `安盛街.js`：帆布袋入口副作用改 `_bagExtra`，系统提示补「获得帆布袋」。

**关键设计点（与用户原方案的偏差，已确认合理）**：
1. 用户说「若当前容量 >= 可更换的包的容量就不显示」——但包本身不占容量（是容器），所以**闸门改用 `vars._bagTier < N`**（当前档位 < 该包档位才显示），避免"已背书包还提示换双肩包"。
2. 用 `set: { _bagTier: N }` 而非 `add`，天然实现「换包是换不是加」。双肩包→书包 得到 5 而非 6。
3. 三处帆布袋入口**不再互斥**（这是原缺陷的根因），但仍共享 `hasBag` 所以只能拿一个（符合"只能带一个袋子"）。

**容量上限**：书包5 + 帆布袋1 = 6 格（原为 4 格）。

**新增回归工具**：`tools/bag_volume_selftest.js`（27 断言）。用 vm 沙箱加载真实 core/utils/4 个剧情文件，自建迷你引擎（深拷贝 + computed 注入 + showCondition 求值）。覆盖：默认3、帆布袋+1、双肩包→4、书包→5、换包不叠加、包+袋叠加、闸门可见性、丢袋回退、三入口并存、旧档兼容。

**连带修复**：core.js 插 5 行变量 → `stamina_report.py` KNOWN_SITES 的 core.js 区间再 +5 平移（饥饿358-364、疲劳381-390、喝水1121-1130、冻肉1160-1167、维C1253-1265、进食1171-1252）；selftest mock 行号 373→388、1157→1162。踩坑：mock 行号必须落在区间内，先设 378（区间外）直接 FAIL。

**全量回归全绿**：condition_audit 0/0、unarmed_fight 34/34、bag_volume 27/27、stamina_report --selftest 22 条、stamina_audit 174 变动点（±130 平衡）。5 个改动文件 `node --check` OK。

**文档同步**：CLAUDE.md 变量表更新 + 新增「背包容量体系（主背包 + 袋子）」章（含标准写法与三条要点）。

## 补充：上实南校书包入口（同日，用户追加需求）

- 用户要求把 `上实南校-1号楼走廊`（原文案「书包、水杯散落一地」）也做成可捡书包，与建平 3F 高一教室一致。
- **难点**：该走廊是**单向过场节点**——原文案"你没有时间细看，侧身闪进最近的一间办公室"，且原有出口全部指向 `上实南校-天桥`（不返回走廊），所以不能像建平那样让结果节点回到原场景。
- **解法**：新增结果场景 `上实南校-1号楼走廊-捡书包`，`nextScene: "上实南校-天桥"`（继续向前的唯一路径）；`positionAfterOperation` 仍设回走廊（防御性，防被意外返回时错位）。
- 走廊文案微调：删掉"你没有时间细看"（否则与"顺手捡包"矛盾），改为"你侧身闪进最近的一间办公室"。
- **choices 从数组式改为函数式**以支持条件插入，原 `showCondition: "hasCutter"` 改为 `if (vars.hasCutter)`（语义等价）。
- 闸门用 `vars._bagTier < 2`（与建平一致），故两处书包入口天然互斥、不会重复加成。
- 回归断言 27 → **33**（新增 4b 组 6 条：出现/原有选项不受影响/结果节点存在且返回天桥/容量→5/已换后隐藏/跨入口互斥）。全绿。

## 环境诊断：Bash 「dirname/ls/python not found」根因确认与解法

**现象**（用户转述此前 AI 反馈）：Bash 里 `dirname` / `ls` / `python` 全部 `command not found`，基本不可用。

**核实结论：属实，但根因不是 PortableGit 装坏了，而是缺 PATH 条目。**

- PortableGit 安装**完整无损**：`C:\Users\wgdin\.workbuddy\binaries\PortableGit\versions\1.2.0\` 下
  `usr/bin`（含 `dirname.exe` / `ls.exe`）、`cmd`、`mingw64` 均在；绝对路径调用 `dirname.exe`、`ls.exe` 正常。
- **真正原因：PATH 里没有 PortableGit 的 `usr/bin`。** PATH 中唯一沾 Git 的是 `D:\Program Files\Git\cmd`，
  而 `cmd` 只有 `git.exe`，**不含任何 coreutils**（ls/dirname/tr/grep/sed/awk/wc/head 全无）。
  系统 `D:\Program Files\Git\usr\bin` 存在但**同样不在 PATH 上**。
- 结果：bash builtin（`echo`/`cd`/`export`/`[` 等）能用，**所有外部命令一律 not found**，node/python 一并失效。
- **附带发现：PATH 格式被污染** —— 原文应为
  `D:\WorkBuddy\resources\app.asar.unpacked\cli\vendor\shim\safe-bin` 的条目，被 `:` 切成了 `D` 和
  `\WorkBuddy\...` 两条（Windows 盘符 `D:\` 被当分隔符）。同源问题：`BASH_ENV` 是 Windows 反斜杠格式
  `D:\WorkBuddy\...\shell-runtime-bash-env.sh`，bash 能打开它，但文件内第 3 行
  `$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)` 因 dirname 不可用而失败，于是**每条命令都往 stderr 打两条噪声**：
  `dirname: command not found` + `cd: null directory`（无害，可忽略）。

**已验证解法**（命令开头 export 即可，不用改任何配置）：
```bash
export PATH="/c/Users/wgdin/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:/c/Users/wgdin/.workbuddy/binaries/PortableGit/versions/1.2.0/bin:$PATH"
```
加完后 dirname/ls/tr/grep/wc/sed/awk/python/python3/date 全部恢复；`node tools/condition_audit.js` 跑通（0/0）。
**操作约定**：本项目里任何需要外部命令的 Bash 调用，开头都先 export 这行，否则一律 not found。

**排查手法备忘**：环境类「工具全 not found」优先怀疑 PATH 而非二进制缺失——用 `[ -e "$p" ]` 逐个探测
绝对路径可快速区分「装坏了」与「没上 PATH」，比 Glob 可靠（Glob 对含中文的 U 盘路径 + 深层目录
返回 No files found，会误导成目录不存在）。

## 追加：用户改系统 PATH 后仍无效 —— 定位到 WorkBuddy 自身 bug（同日 13:17）

**用户操作**：把 `D:\Program Files\Git\usr\bin` 加进系统 PATH，重启 WorkBuddy。
**用户自测**：在**自己打开的 Git Bash** 里全部正常（dirname/ls/grep/tr/wc/sed/awk/date 全 OK；python=3.8.10、node=v25.2.1 ——
注意这是系统 Python38 和系统 node，与 WorkBuddy 托管版本不同，证明那是**另一个进程**）。
**但 WorkBuddy 的 Bash 工具里仍然全部 not found**（python/node 却是好的）。

**根因（已定位到源码）**：`D:\WorkBuddy\resources\app.asar.unpacked\cli\dist\codebuddy.js`
里的 `buildPluginPathEnv()`：

```js
function buildPluginPathEnv(eA){
  let el = process.env.PATH || "";
  if("win32" !== process.platform) return [...eA, el].filter(Boolean).join(":");
  let {shell: ec} = getShellConfiguration();
  if(ec === "bash" || ec === "zsh" || ec === "sh"){
    let toUnix = a => windowsPathToUnix(a);
    return [...eA.map(toUnix), ...el.split(";").filter(Boolean).map(toUnix)].join(":")
  }
  return [...eA, el].filter(Boolean).join(";")
}
```

**缺陷**：Windows 盘符含冒号。`windowsPathToUnix("D:\\Program Files\\Git\\cmd")` 产出 `/d/Program Files/Git/cmd`，
但转换若未完整处理（或含残留反斜杠的段），`join(":")` 后 bash 就会**在盘符的冒号处错切**——
这正是 PATH 开头那两条碎片 `[D]` + `[\WorkBuddy\...]` 的成因（本应是
`D:\WorkBuddy\...\shim\safe-bin` 一条）。**必须用 `/d/...` 开头的 POSIX 路径，不能靠 `windowsPathToUnix` 半吊子转换。**

**实测证据（bash 内 PATTERN 匹配）**：
- `case ":$PATH:" in *"Git/cmd"*)` → **YES**（旧的 `Git\cmd` 进了 PATH）
- `case ":$PATH:" in *"Git/usr/bin"*)` → **NO**（新加的 `Git\usr\bin` 没进）
- PATH 共 38 条，后半段是原样保留的 Windows 风格条目（`/d/Program Files/Python38/` 等带尾部斜杠）
→ 结论：**用户新加的条目在注册表里，但没能通过 `buildPluginPathEnv` 的转换/拼接进入 bash 会话**。

**另注**：`reg.exe` 被安全策略拉黑（Program Blacklist），**不能用 reg query 读 PATH 注册表**；
`PowerShell` 工具的 stdout 在本环境也未被捕获（Exit 0 但无输出）。
→ 读环境变量只能靠 bash 侧的 `case`/`[ -e ]` 探测，别绕道 reg/PowerShell。

**解决方案（按推荐度）**：
1. **最省事且已验证**：脚本/命令开头 export PortableGit 的 `usr/bin`（无空格，转换不会出问题）：
   ```bash
   export PATH="/c/Users/wgdin/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin:$PATH"
   ```
   实测 coreutils + python 3.13.14 + node v22.22.2 全恢复，`condition_audit` 0/0。
2. 若想一劳永逸：在 `~/.bashrc` 或 WorkBuddy 的 shell snapshot 里加同一行 export（改的是用户配置，非应用安装目录）。
3. 直接改 `buildPluginPathEnv` 修 bug —— **不推荐**：`app.asar.unpacked` 属应用安装目录，会被更新覆盖。

## ⚠ 最终解法：改 BASH_ENV 指向的 shim 脚本（同日 13:25 落地并验证）✅

**关键坑：`~/.bashrc` 对 WorkBuddy 的 Bash 工具完全无效！**
- 实测 `$-` = `hBc` → **非交互、非登录** shell。bash 只在**交互式**才自动读 `~/.bashrc`，
  所以 WorkBuddy 每条命令都**不会**加载它（已用「写入后新命令仍 dirname not found」实证）。
- 已给用户创建 `C:\Users\wgdin\.bashrc`（内容含 coreutils PATH 前置，带存在性判断和去重），
  **它只对用户手动开的 Git Bash（交互式）有用**；实测 `bash -i -c 'dirname ...'` 生效，普通调用不生效。
- 同理 `etc/bash.bashrc`、`etc/profile` 也都不管用（非登录非交互）。

**真正生效的注入点：`BASH_ENV`。** 非交互 bash 唯一自动 source 的就是它。
WorkBuddy 注入的值 = `D:\WorkBuddy\resources\app.asar.unpacked\cli\vendor\shim\shell-runtime-bash-env.sh`
（该脚本第 3 行 `dirname` 报错，恰好证明它【每次都被执行】）。

**已实施的修复**（文件可写，已先备份为 `shell-runtime-bash-env.sh.bak-20260921`）：
在该脚本**最顶部**（`__codebuddy_shell_runtime_dir=...` 之前）插入：

```bash
__codebuddy_pg="/c/Users/wgdin/.workbuddy/binaries/PortableGit/versions/1.2.0"
if [ -d "${__codebuddy_pg}/usr/bin" ]; then
  case ":${PATH}:" in
    *":${__codebuddy_pg}/usr/bin:"*) ;;
    *) PATH="${__codebuddy_pg}/usr/bin:${__codebuddy_pg}/bin:${PATH}" ;;
  esac
fi
export PATH
unset __codebuddy_pg
```

**验证结果（无需任何手动 export）**：dirname/ls/grep/tr/wc/sed/awk/date/python/node 全部可用，
且 **stderr 两条噪声（`dirname: command not found` + `cd: null directory`）彻底消失**——
因为 PATH 修好后，脚本第 3 行的 `$(cd "$(dirname ...)" && pwd)` 终于能正常执行。
注意：**必须插在第 3 行之前**，否则第 3 行仍会在 PATH 修好前执行而报错。

**项目回归全绿**：condition_audit 0/0、unarmed_fight 34/34、bag_volume 33/33、
stamina_telemetry 9/9、stamina_report --selftest 22 条。

**⚠ 持久性风险**：该文件在 `app.asar.unpacked`（应用安装目录），**WorkBuddy 更新后可能被覆盖**。
回滚：把 `.bak-20260921` 覆盖回去即可。若更新后症状复发，重跑本节插入即可。

## 战斗失败惩罚全库审计（同日 19:30，新增工具）

**产出**：`tools/战斗惩罚审计报告.md`（288 行）+ `tools/combat_penalty_report.js`（可复跑）。

**做法**：vm 沙箱加载 24 个剧情文件 → 遍历场景/选项/`qte` → 四类战斗点识别 → 失败分支目标节点性质判定（死亡结局 vs 受伤 vs 软失败）→ 提取惩罚数值。`effOf()` 同时汇总「节点 onEnter」与「节点内选项 effect」（很多"犹豫"节点把扣体力写在选项上，只看 onEnter 会漏）。

**核心数据**：114 处有失败分支的战斗点 = A颜色QTE 45 + B体力门槛 19 + C其他 3 + D场景级限时QTE 47；失败目标节点 75 个 = 死亡结局 41 + 受伤续玩 22 + 软失败 12；胜利也扣体力的节点 29 个。

**关键机制备忘**：
- 成功成本 `combatCost`（utils.js:367）：`meleeWeaponTier >= 2 ? 1 : 2` —— 铁管/拐杖/匕首/斧头扣 1，空手/美工刀/拖把杆扣 2；`combatDrain` 挂胜利节点 onEnter，扣到 0 走全局"体力耗尽猝死"。
- 失败受伤包：体力 −1~−3（黑皮丧尸 −3 最重、滑扶手 −1 最轻）+ 汞负荷 +10~+15 + `hurtByZombie` + `tryBreakWeapon` 概率断武器（弱 50% / 中 25% / 强 10%，空手不损）。
- 死亡结局 onEnter **全为空**，失败即跳结局、无任何数值交代。
- 惩罚落差：仁济南院颜色 QTE 失败=受伤续玩，建平/张江同机制失败=直接死亡（全库最大的体验割裂点）。
- **路径封锁与战斗失败无关**：失败不锁路；真封锁是战斗前置闸门 3 类——堵路楼梯间（`jpBlockedStair`，徒手完全不可过，需斧/枪/匕首/火把）、尸潮等级闸门（`chasedByZombies <= 3`，可躲藏降级恢复）、剧情通行证闸门（`_hasPoliceMap` / `_visit[场景]>0`）。
- 隐藏坑：`地下车库的丧尸` 选"脚"满足 `strength > 1` 仍 → `结局-嘎吱嘎吱` 死亡（门槛通过但选择错误即死）。

**脚本踩坑（再犯易错点）**：`isEnding()` 不能用「无 choices 且无 nextScene」判定——`travelScene` 等工厂生成的场景 choices 是函数，在桩 state 下可能返回非数组，会被误判成结局（曾把 `前往金谊广场-2` / `张江-河北岸-街口` 误标为死亡）。**必须改为「id 以 结局 开头 或 文案含 `结局：`」**。

## B 类体力门槛失败结局拆分（同日 19:55，落地 5 处）

**用户判定标准**：`strength >= N` 门槛战斗的失败结局，**只给"被动迎战"的情节**拆出"体力专属结局"，委婉暗示玩家是没力气才打不过；**玩家主动攻击的情节不拆**（玩家死了会认为是自己作死，怪不到体力门槛上）。受伤/死亡不统一的问题用户暂不改（死亡可立即回溯，负担不大；受伤反而更麻烦，需及时疗伤/进食）。

**核心做法（⚠ 关键约束）**：新增结局节点、ID 加 `-力竭` 后缀，**只把体力分支的 elseScene 改指新节点，原结局节点一律不动**——因为多个结局是**共享节点**（`结局-被丧尸扑倒咬死` 源码注释即写明"共享节点，覆盖所有汇入此处的战斗失败/QTE超时"，樱桃苑内十几处 QTE `onTimeout`/`nextScene` 都指向它；`结局-安盛街-试衣间丧尸扑脸`、`结局-安盛街-被尸潮吞没` 也各被 QTE 超时共享）。**直接改文案会污染非体力来源的死亡**。**结局显示名与原结局保持一致**（暗示靠文案，不靠结局名点破）。

| 文件 | 发起场景（门槛） | 原 elseScene | 新节点 |
|---|---|---|---|
| 樱桃苑（初始小区）.js | 装睡的丧尸「先躲回家」(`strength > 2`) | 结局-背后的偷袭 | 结局-背后的偷袭-力竭 |
| 樱桃苑（初始小区）.js | 地下车库的丧尸 脚/头/身体（3 分支） | 结局-被丧尸扑倒咬死 | 结局-被丧尸扑倒咬死-力竭 |
| 东明社区图书馆.js | 图书馆-阅览室-惊醒「推倒书架」(`>= 3`) | 结局-图书馆-书堆 | 结局-图书馆-书堆-力竭 |
| 安盛街.js | 服装店试衣间「一脚踹过去」(`>= 3`) | 结局-安盛街-试衣间丧尸扑脸 | 结局-安盛街-试衣间丧尸扑脸-力竭 |
| 安盛街.js | 被包围「硬闯缺口」(`>= 4`) | 结局-安盛街-被尸潮吞没 | 结局-安盛街-被尸潮吞没-力竭 |

**文案手法**：用身体失控间接点出没力气——"双腿沉得厉害，膝盖像灌了铅" / "手臂在发抖，使不上力" / "抬脚踹过去，可这一脚轻飘飘的" / "大腿酸得发软，一头撞进人墙"；**不直说"体力不足"**。初稿误用"纹丝不动"（AI 禁增词），已改"怎么也推不动"。

**实现细节**：地下车库 3 个 elseScene 文本完全相同，用 `replace_all` 一次改完（该精确串在同文件仅此 3 处）；新节点 `结局-被丧尸扑倒咬死-力竭` 保留原节点的 `tryBreakWeapon` onEnter + `weaponBrokeText(vars)` 函数式文案以保持惩罚一致。

**跳过清单与理由**：主动攻击类（图书馆-大厅「上去解决它」、益丰柜台后「给它来一下」、安盛街收银台「砸过去」、公交站「打开巴士门」）；文案已暗示体力类（地铁站-站厅层/楼梯/滑扶手、图书馆藏书区扭打、安检区斧子太重）；非战斗类（张江-华大-风淋舱-击退 `strength > 0.01`，是风淋舱互锁 + 全局体力耗尽的变体）。

**回归**：`condition_audit.js` 0/0、`unarmed_fight_selftest.js` 34/34、`combat_penalty_report.js` 复跑（失败目标节点 75→78，死亡结局 41→44，B 类表已正确指向 `-力竭`）。新节点引用计数 2/4/2/2/2 全部正确。

**孤儿节点已清理（19:58 追加）**：`结局-背后的偷袭`、`结局-图书馆-书堆` 因唯一引用被改指后无任何引用，按用户指示**已删除节点定义**（删后 `node --check` 通过、`condition_audit.js` 0/0、`combat_penalty_report.js` 复跑 78/44 不变）。**删除手法**：旧 ID 与 `-力竭` 新 ID 会互相子串干扰，`grep` 校验必须带引号做精确匹配（`grep -rn "\"$ID\"" `），不要用裸 ID。

---

## 场景级 QTE 分布统计（20:0x）

**新增工具 `tools/qte_report.js`**（vm 沙箱加载 24 个剧情文件 → 遍历 storyData → 90 处场景级 QTE + 41 处选项级 QTE），产出 `tools/场景级QTE分布报告.md`（7 章）与 `tools/qte_report.json`。改 QTE 后复跑即可。

**口径**：场景级 = 场景对象自带 `qte`（engine.js:822）；选项级 = `choice.timeout`（闪色输入题）。

**结论**：场景级 90 处 / 14 个区域。写法：静态对象 40 + 追逐工厂 42 + 条件动态函数 4 + 过场工厂 travelScene 4。区域集中度：新达汇 28 + 建平 16 = 44（49%）。显示进度条 79 / 隐藏 11；有选项 85 / 无选项 5。静态型时限 2–20s，均值 9.4s。超时去向高度集中：`结局-被丧尸扑倒咬死` 12 + `结局-丧尸的围殴` 7。选项级 41 处，张江独占 14（张江**无任何战斗型场景级 QTE**）。

**踩坑记录（写脚本时的三个真坑）**：
1. **同名场景 ID 会在多文件出现**——用「文件里是否包含该 ID 字符串」定位定义文件必然误归因（如 `三林路` 被 utils.js 的 NAV_TABLE 抢先命中，`安盛街东侧` 被东明街道路径.js 的 nextScene 抢先命中）。正解：按行首 key 定义模式定位（`^\s*"id"\s*:` 且后跟 `{` 或标识符）。
2. **工厂生成场景在源码里没有字面 `qte:`**——`travelScene()` 生成的 4 处、`qte: jpChaseQTE()` / `mallQTE()` 生成的 42 处，靠正则找 `qte:` 会全部漏掉。正解：以**运行时 `typeof node.qte`** 判定对象/函数，源码提取只作描述用（读场景值时用括号配对取整段表达式，别只找第一个 `{`）。
3. **动态 QTE 的探测状态要多种组合**——单靠「压力态（ch=3 + _visit 全 1）」会让守卫条件为"已探索"的 QTE 反而不触发（如 `安盛街-食品店内部` 需 `!_visit[...]`、`建平-后门-内侧` 需 `_visit清场===0`）。正解：按 (ch=0/3) × (_visit=0/1) 四种合成态各探一次，取首次触发的作基准、ch=3 的作满追值。

---

## 长者食堂饮水机：查看/接水拆分（21:40）

**问题**：`长者食堂-饮水机` 是「查看」与「使用」混一的节点——同一个 `text` 里既描述饮水机状态，又在第 4 分支直接写"你把空水瓶放到出水口下面，按下出水键"，且"接水"选项 `nextScene` 指回自己（自环）。

**改法（沿用本文件 `长者食堂-窗口` → `长者食堂-窗口-喝汤` 的既有范式）**：
- `长者食堂-饮水机`（查看）——`text` 只保留状态描述 4 分支；原第 3 分支"你**按下出水键**，但只流出几滴"（空箱态，只能靠"看看饮水机"进入才有机会看到）改为纯观察口径："接水盘却是干的 + 掀开蓝色水位窗→水箱见底、指示灯变红"。
- 新增 `长者食堂-饮水机-接水`（动作）——`onEnter: { add: { _waterDispenserUses: 1, bottleWater: 1 }, set: { waterToxic: false } }`，接水文案 + 单一「继续」回查看节点。
- **时间口径不变**：仍为「查看 1 分钟 + 接水 2 分钟 = 3」，即 `updateTime(2)` 从原"接水"选项平移到新的"用饮水机接水"选项上，「继续」免费（与 `窗口-喝汤` 一致）。
- 闸门 `hasBottle && bottleWater == 0 && _waterDispenserUses < 10` 原样保留（满 10 次仍隐藏接水项），故接水节点内不存在"按了却没水"的分支。

**约定**：本文件场景内"查看某物"的节点只写状态，"对某物动手"一律拆成 `-动词` 子节点承载状态变更（onEnter）+ 动作文案 + 回跳。新增节点后无需改 `_variables`（`_waterDispenserUses` 已在 core.js:127）。

**回归**：`condition_audit.js` 0/0、`graph_audit.mjs` fnErrors=0（1396 节点 / 3095 边，新节点 n10 连线 内部n5→n9→n10→n9 正确）。`lint_story.mjs` 仍为 E=1（樱桃苑 B1 空 text，既有遗留，与本次无关）。

