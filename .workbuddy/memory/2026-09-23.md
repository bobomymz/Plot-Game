# 2026-09-23 工作日志

## 审计：汞负荷（mercuryLoad）机制
- 新增工具 `node tools/mercury_scan.js`（可回归）+ 报告 `tools/汞负荷机制审计报告.md`。
- **全库 43 处引用 = 41 写入 + 2 读取**。写：加汞 39 处（`+10` 37 处 / `+15` 2 处 / `+5` 2 处 / `+20` 1 处）、减汞 1 处（童涵春堂药丸 −20）。读：仅 `core.js:504` 全局触发器 `>= 70`、`core.js:899` 药丸选项门槛。
- **P0 真 bug：`mercuryLoad` 未注册进 `_caps`**（core.js:316 只有 strength/chasedByZombies/phoneBattery）→ `clampAll()` 对它无效。41 个写入点只有 **5 个**自带 `Math.min(100)`（夜晚剧情 2、张江 3），**36 处裸 add 无上限**。core.js:19 注释的「0-100」只是口头约定。
  - 影响面：因触发条件 `>= 70` 是即时判定，越界**不改死亡结论**；但会让「药丸 −20 能否救回」失真，且存档可能 >100。
  - 建议：`_caps` 加 `mercuryLoad: { min: 0, max: 100 }`——一处改动、零平衡影响。**待波波拍板**。
- **P1 设计缺口：中间档体征全未实装**（设计细节 §三 规划 20-40 皮肤灰白 / 40-70 痛觉消失+夜视）。全库检索 `汞` 字确认，除金谊广场消毒处一句「已进入体内的汞不会因此消失」（金谊广场.js:1171）外，**0 到 69 全程无任何玩家侧反馈**，第 70 点瞬时暴毙 → 体验是「凭空暴毙」，与「难度不该来自信息不透明」冲突。**属平衡决策，只记录不动手**。
- 周边观察：`结局-汞中毒尸变` 有 2 处**硬编码直跳**（金谊广场.js:668 未戴面具深入 B2、:1334 未戴面具在车库），**与变量无关**，0 点汞也能到——合理的「环境毒气即死」，仅提示勿与变量机制混淆。
- 上实南校化学实验室 `+10` 是全库唯一「只加汞不置 hurtByZombie」点（注释明确化学灼伤非咬伤），逻辑自洽。
- **通用教训**：本库的「隐藏累积型变量」（如 mercuryLoad）**只看条件而看不到上限注册**，`_caps` 漏注册这类 bug 不会报错。以后再查此类变量，必须同时 grep `_caps`。

---

## 实装：汞中毒系统改造（作者拍板后落地）

三项全部实装，**完整回归全绿**。报告 `tools/汞负荷机制审计报告.md` §九。

### 1. P0 修复：`_caps` 注册
`core.js` `_caps` 加 `mercuryLoad: { min: 0, max: 100 }`。36 处裸 add 从此受钳；药丸 −20 不会再"减不动"。

### 2. 慢性累积：`mercury-chronic` 规则（作者定速 **每小时 +1**）
- **关键设计**：`condition: "mercuryLoad > 0"` —— **被咬/喝毒水才启动**，未暴露者永不累积。因果链：**被咬是开关，时间是放大器**。避免"没被咬却被汞杀死"的不可归因死亡。
- 台账 `_mercuryChronicHour`（规则自维护）；`triggerKey: Math.floor(gameMinutes/60)`；跨 N 小时一次补 N 点。
- 节奏：load=10 → **60h ≈ 2.5 天**到 70；load=20 ≈ 2.1 天。

### 3. 三级隐性体征（**不进 flashStatusWarning**，全走正文）
新增派生变量（`_reactive.computed`，审计脚本自动注入，**无需改脚本**）：`mercuryTier`(0/1/2/3，分档 0/20/40/70)、`noPainSense`(=tier≥2)、`hasDimLight`(手电∨手机有电∨高汞夜视)。判定函数 `mercuryTier(load)` + 旁白池 `mercuryPainNote(vars)` 在 `utils.js`。

| 档 | 症状 | 落点 | 手法 |
|---|---|---|---|
| 20-40 | 皮肤灰白 | 新达汇-2F卫生间（裂镜）、理发店内部（镜子） | 只在**能照见自己**的载体触发 |
| 40-70 | 痛觉消失 | 金谊广场-地铁站厅-失败 / 图书馆-阅览室-徒手 / 地铁站-安检区-硬冲 / 建平-致真楼-1F-老吴杂物室 / 樱桃苑-4楼-失败 | **文案反转**："火辣辣地疼"→"你先看见的是血，不是疼"；颜色用灰 `#9aa0a6`，与橙色系统提示刻意区分 |
| 40-70 | 夜视增强 | 菜市场-员工通道/通道迷路、新达汇-B1值班过道/B1保安室/卡通尼/2F北走廊中、上实南校-3号楼-4楼-摸黑 | **正向诱惑**：黑暗中"看得清"像好事，玩家以为变强 |

**夜视弱于手电的三个约束**：① 一律"只辨轮廓，看不清文字"（每节点显式写明），**不能替代手电**；② 真实机制收益仅 1 处——`新达汇-2F北走廊中` 椅子选项 `showCondition` 由 `canSee` 放宽为 `(canSee || noPainSense)`；③ 1 处代价减免——`上实南校-3号楼-4楼-摸黑` 体力 −1 改为仅非高汞时扣。

### 4. 新增两个回归工具
- `node tools/mercury_selftest.js`（**37/37**）：只载 utils+core，档位边界 / _caps 钳制 / 慢性累积 / 旁白 / 弱光三通道 / 药丸。
- `node tools/mercury_engine_e2e.js`（**20/20**）：**无头加载真实 engine.js + 全部 24 个剧情文件**（DOM 桩），跑通 initGameState/applyReactive/applyEffect/checkCondition/真实 text。**浏览器不可用（playwright 未装 chromium）时的替代 E2E 方案，可复用。**
- `tools/mercury_scan.js` 同步更新（`_caps` 从 ❌ 转 ✅）。

### 5. 完整回归结果（全绿）
mercury_selftest 37/37｜mercury_engine_e2e 20/20｜condition_audit 0/0｜scene_fn_selftest 0 异常｜stamina_report --selftest 22 条｜telemetry 9/9｜unarmed 34/34｜bag 33/33｜bottle 72/72｜overnight 悬空0/失效0。

### 6. 新踩坑（写进 skill）
- **无头引擎验证的两个 Node vm 陷阱**（浏览器里不存在，勿误判为游戏 bug）：① `engine.js` 的 `evaluateExpr` 用 `new Function(...Object.keys(vars))` 求值，**该作用域看不到 utils.js 顶层函数**，vm 里必须显式挂到 `globalThis`（浏览器靠全局作用域天然可见）→ 否则 `fatigueTier` 之类假报未定义；② **直接调 `node.choices(vars)` 会拿到全部选项**，`showCondition` 由引擎在 `engine.js:1026` 另行 filter，验证选项可见性必须自己复刻那次 filter（否则会误判"选项凭空出现"）。
- playwright E2E 在本机不可用：`chrome.exe` 报 `Invalid file descriptor to ICU data`，自带 chromium 未安装。**改用无头 engine 方案替代**。


## 体力遥测 KNOWN_SITES 行号同步（core.js +1）
- 波波在 core.js:320 加了 `mercuryLoad: { min: 0, max: 100 }`（正是上面 P0 的修复），位置在 `_caps` 区（**晚于 `_variables`**），只会让**其后的** core.js 区间平移 +1；`_variables` 给水位线之上的（饥饿 361–367 / 疲劳 383–393）只有之前的水瓶插入影响，本次不受动。
- 已同步 tools/stamina_report.py 的 KNOWN_SITES：core.js 七个区间全部 −/+ 校正为真实地址（饥饿 361–367、疲劳 383–393、喝水 1124–1133、冻肉 1163–1170、维C 1256–1269、整理整理 1174–1255），并补了 2026-09-23 的修改记录注释。
- 回归：stamina_report.py --selftest 9/9、stamina_telemetry_selftest.js 9/9。engine.js 本次未动，无需改。
- **定位心法**：这个映射只需要抓住「入口函数/规则所在的行」，不必逐行精确——`core.js:1420` 这类 `{}` 闭包体内**无函数调用帧**，测出来的行号是 Proxy trap 的，归不到源上；会进日志的只有「通过函数/规则执行」的写入点。

## stamina_report.py 改为「按内容锚定」（波波拍板）
- 废弃硬编码 `KNOWN_SITES` 行号区间，改为 `CONTENT_ANCHORS` + 运行时解析：每次运行先扫 story/ 与 engine.js，按锚点定位入口行再划区间。
- 锚点类型：`fn`（函数声明，utils.js 的 restRecover/applyWeatherDrain/sprintAway/combatDrain/hideOnLocation）、`rule`（core.js 的 `id: "starvation"` / `id: "travel-fatigue"`）、`scene`（`"整理整理-喝水"\s*:\s*\{`）、`range`（场景键首尾）、`line`（engine.js 两处对象式效果、夜晚剧情保底）。
- **踩坑记录**：场景锚点一开始用裸 `"整理整理-喝水"`，**先撞到 core.js:626 的 `nextScene` 字符串引用**（不是场景定义）→ 必须带 `: {` 后缀。这与 MEMORY.md「`_visit` 键名必须等于真实场景 ID」是同一类陷阱：字符串引用 ≠ 定义处。
- 新增 `--list-anchors`（打印解析结果，标 ok/MISS）；报告头部显式展示「锚点解析：命中 N / 未命中 M」，未命中时警示并落回原始 `文件:行号`。
- **自测同步升级**：`--selftest` 的合成数据改用「运行解析出的真实行号」（不再写死 643/370），这样锚点漂移会连带自测报警；另加 3 项锚点断言（全命中 / 关键标签都在 / 区间行号合法）。18/18 通过。
- **抗漂移实测**：临时在 core.js 插 5 行 → 锚点自动 +5 跟随（361→366、1121→1126…），`--list-anchors` 仍 15/15 命中；已复原（1403 行、无残留）。⇒ 以后增删行**不用再手动同步**。
- 回归：`--selftest` 18/18、`stamina_telemetry_selftest.js` 9/9、`node --check engine.js` OK。
