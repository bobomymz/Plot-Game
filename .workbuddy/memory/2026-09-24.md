# 2026-09-24 工作日志

## 忻老师章节（复旦江湾）完成度核查

**结论**：代码层完成度 ~100%（阶段3 正文全部落地，可通关）；文档层 3 处未收口。

### 关键发现：审计工具假绿（已修）
- 9 个审计脚本硬编码的 `FILES` 清单**漏载 `story/复旦江湾.js`** → 本章此前**从未被任何审计覆盖**，
  打印的「0 处」是"没扫到"而非"没问题"；`scene_fn_selftest` 还连带误报该文件定义的场景「不存在」。
- 已补清单：`condition_audit` / `scene_fn_selftest` / `overnight_shelter_audit` / `qte_report` /
  `combat_penalty_report` / `combat_stamina_feedback_audit` / `sprint_away_audit` / `bottle_scan` /
  `bottle_pickup_selftest`。补后仍全绿 → 剧情本身没问题。
- 根治建议（未做）：改成解析 `index.html` 的 `<script src="story/...">`。

### 新增工具
- `tools/xin_chapter_check.js`（55 断言，全绿）：无头真引擎实走章节 —— 2×2 分支矩阵、
  a₁/a₂ 双出示窗口、空口安慰陷阱、`jpXinFuse` 延迟引信（③次日/④隔日/给药不炸/结局路由）、
  missable（`_xinGone`）、整理整理给药位置门控、仁济第二入口、24 节点 × 四组合零选项扫描。
- 报告：`tools/忻老师章节完成度报告.md`。

### 文档修正
- `人物档案.md` 王知筠 305 节两处「未实装/占位」→ 改为「已实装 2026-09-24」（事实性过期）。

### 踩坑（已写进 skill）
1. 新增剧情文件后必须同步所有脚本的 `FILES`，否则假绿；
2. 无头加载真 engine.js 时**不要给 sandbox 传外层 `Function`** —— `evaluateExpr` 的
   `new Function` 会拿到 Node 外层全局作用域，假报 `fatigueTier is not defined`；
3. `initGameState()` 会整体替换引擎全局 `gameState`，Node 里捕获的引用会变味 → 用 `let` + 重新取。

### 回归读数（含本章）
condition_audit 0/0 ｜ scene_fn_selftest 0 异常 ｜ overnight 悬空 0/失效 0 ｜ sprint_away 0 P0 ｜
mercury 37/37 + 20/20 + leak 0 P0 ｜ unarmed 34/34 ｜ bag 33/33 ｜ bottle 72/72 ｜
stamina 自测通过 + 遥测 9/9 ｜ bridge 22/22 ｜ area_check 三连全绿 ｜ **xin_chapter 55/55**

### 遗留
- `人物档案.md` 仍**无忻老师条目**（DoD 自列，待拍板人设后落笔）。
- `CLAUDE.md` 是否补 `_phoneOrigin` 行；差异矩阵文件位置未找到。
- 14 张章节图仍 placeholder。

---

## 收口（2026-09-24 第二轮，波波指派 1/2/4）

### 1. `人物档案.md` 新增 016 · 忻老师
严格按代码已落地部分写作：身份/位置/家属/住址/载具/四分支/③④后日谈/抓伤引信 全部有源码出处；
未实装项（全名、年龄、外貌、"复旦招生办联络人"身份、妻女细节）在条目文末单列"未实装（留待补充）"。
同步：标题 001-015→001-016、编号说明、实装状态、人物总表加行。

### 2 & 4. 工具根治（本次重点）
- 新增 `tools/story_files.js`：清单唯一权威 = 解析 `index.html` 的 `<script src="story/*.js">`；
  解析异常（<10 条）回退内置清单**并打警告**（不让审计静默变空跑）。
- 9 个脚本改为 `require('./story_files').list()`（`sprint_away_audit` 因 CORE/REST 两段加载，
  用 `.filter()` 剔 CORE 后重建 REST）。语法校验 + 9 个全部复跑通过 + 无回退警告。
- 结论：**以后新增剧情文件只要挂进 index.html，审计自动纳入** —— "假绿"这一类缺陷根治。

### 文档/矩阵
- 差异矩阵 = `.claude/skills/area-story-design/SKILL.md`（此前以为的"独立文件"其实在 skill 内）；追加复旦江湾行。
- `CLAUDE.md`「照明分级」条补一句：手机有多处拾取点时必须同步写 `_phoneOrigin`（否则 b 链静默失效）。
- `docs/区域方案-复旦江湾.md` DoD 逐项勾选 + 新增「阶段4 收口记录」。
- `tools/忻老师章节完成度报告.md` 结论/遗留按收口结果更新。

### skill 更新
`shichao-story-variable-audit`：骨架代码改用 `story_files`；坑位速查"FILES 不同步"条目改为"已根治"；
历史记录追加当日收口。

### 回归
condition_audit 0/0 ｜ scene_fn_selftest 0 异常 ｜ overnight 0/0 ｜ sprint_away 0 P0 ｜
xin_chapter 55/55 ｜ area_check 复旦江湾 三连绿（lint E=0 / W=2 假阳性）。

### 仍待拍板
- 蔡镜晓 / 彭奕宸 / 刘冠宇 三人独立条目 + 章节动向补记（DoD 同项要求，本轮未做）。
- 14 张章节图 placeholder。
- `MEMORY.md` 已约 16k 字符（超注入上限 3000），建议下次按主题精简并删旧日志。

---

## bug 追查：续玩旧档报 `_xinOutcome is not defined`（当日第四轮）

### 根因（不是忘注册）
`condition_audit.js` 两个 0 → 变量注册无误。真因是**状态**不是**源码**：
`loadSave()` → `applySave()`（原 `gameState = __wrapState(saved.gameState)`）与 `backtrack()`
**整块接管旧状态、绕过 `initGameState`** → 旧档没有江湾章节新增的 10 个键 →
字符串条件的 `new Function(...Object.keys(gameState))` 缺形参 → `ReferenceError` 被吞成 `false`。
**只有字符串条件会报错**（函数式条件写 `vars.xxx` 只拿到 `undefined`），所以"全库一条报错"是假象。

顺手查出**第二个洞（同一根因）**：`_reactive.computed` 的 15 个派生变量同样是"引擎算出来的键"。
实测不补时全库 **25 条**字符串条件报错（含 `zombieAtHomeDoor` / `minutesBetweenReduceStrength` /
`hasNoTransportation`）—— 而 `hasMeleeWeapon` 按项目写法本就**直接写在字符串条件里**，属在册隐患。

### 修复（只动恢复路径，engine.js）
- 新增 `fillMissingDefaults(state)`：补 `_variables` 新键（只补缺、不覆盖）。调用点：`applySave` 的
  `gameState` + **`historyStack` 每一项** + `backtrack`。
- 抽出 `refreshComputed()`（= `applyReactive` 第 1 步，只重算派生量）并在这三条路径调用。
  **刻意不调整个 `applyReactive()`** —— 会提前结算饥饿/疲劳（读档即掉血）。
- **未 bump `SAVE_VERSION`**：新增变量属纯增量，bump = 弃掉玩家进度。只有改字段含义/结构才 bump。

### 新工具
`tools/save_compat_selftest.js`（**27 断言**，含"不补必炸"对照组 + 674 条字符串条件全库扫描）
+ 报告 `tools/旧存档兼容性修复报告.md`。

### 文档
- `CLAUDE.md`「所有变量必须先在 `_variables` 中定义」补第二来源专节（补什么/怎么补/调用点表）。
- `手机端适配.md` `SAVE_VERSION` 约定补**反向条款**：只新增变量不必 bump。
- skill `shichao-story-variable-audit`：核心原理补第 5 条、新增 §2.5 专节、回归口令加该工具、
  历史记录追加；**修正原"老存档无需迁移"的结论**（只对 `add` 生效，不完整）。

### 无头 harness 新坑（写 renderScene 类路径必踩）
- DOM 桩 `mkEl()` 必须带 `className: ""` —— `applyScreenEffects` 读 `overlay.className`，缺了就崩。
- 沙箱要补 `setInterval` —— `renderScene → typeText` 会调。

### 回归（全绿）
condition_audit 0/0 ｜ scene_fn_selftest 0 异常 ｜ sprint_away 0 P0 ｜ rain_image 46/0 ｜
mercury_selftest 37/37 ｜ mercury_engine_e2e 20/20 ｜ mercury_leak_guard 0 P0 ｜
xin_chapter 55/55 ｜ overnight 悬空 0/失效 0 ｜ bottle_pickup 72/72 ｜ **save_compat 27/27**。
`node --check engine.js` OK。

### 待拍板 / 未决
- 波波当前那一局要确认是"旧档"还是"浏览器缓存旧 core.js"（分流命令写在报告 §五）；
  旧档情形刷新即自愈，`_xinOfferDay` 由 `建平中学.js:765` 在下次到后门辅路时补记。

### 追加：重载后的"另外 2 个报错" = 浏览器扩展，不是游戏
波波重载后控制台出现 `index.html:1 Unchecked runtime.lastError` + `Uncaught (in promise) Could not
establish connection. Receiving end does not exist.` —— **扩展的 content script 给已闲置/卸载的
service worker 发消息**（Chromium `kReceivingEndDoesntExistError`）。
判据（已实测）：① 报错源写 `index.html:1`，该文件第 1 行是 `<!DOCTYPE html>`；
② `grep -rn "chrome\.|postMessage|lastError" index.html story/ engine.js` = **0 命中**。
同时游戏自身日志正常（`529 张图片已缓存`），且**上一轮的 `_xinOutcome` 报错没再出现 → 旧档修复在真浏览器里生效**。
已作为"假警报"条目写进 skill 坑位速查（这类红字会跟着"刚改完代码重载"一起冒出来，极易误判成新引入的 bug）。

### 追加：背包容量 `_bagExtra = -1` 根因定位（仁济南院，容量 4→3，捡不了东西）

**根因 = 09-21 13:00 提交 `f9888a5` 改造背包体系时没配套做存档迁移。**
- 旧：`bagVolume` 是可写普通变量（拾取 `add:{bagVolume:1}` / 丢弃 `add:{bagVolume:-1}`）；`hasBag` = **帆布袋/双肩包/书包共用一个**标志（旧档 `bagVolume` 存真实值 4/5）。
- 新：`bagVolume` 变派生值 `3+_bagTier+_bagExtra`（`core.js:357`）；`hasBag` 收窄成"帆布袋"，双肩包/书包改走 `_bagTier`。拾取端与丢弃端**同一次提交一起改**（无半改造窗口）。
- 漏的一环：**`SAVE_VERSION` 前后都是 1**（`git show f9888a5^:engine.js` 与改造后一致）→ 旧档被照常加载。`fillMissingDefaults` 只能补**初值**，`_bagExtra` 初值就是 0，**无法按 `hasBag:true` 反推成 1** ⇒ 容量静默 4→3；旧档 `hasBag` 仍为 true → `整理整理`「丢下帆布袋」**可见可点**（`showCondition:"hasBag"`）→ `0+(-1)` ⇒ **`_bagExtra = -1`**、容量再掉到 2。
- **新开局不可能复现**：不变量 `hasBag=true ⇒ _bagExtra>=1`；全库 `_bagExtra` 写入点仅 4 处、无裸减。故这份档必然来自 09-21 之前（与上一轮忻老师 `_xinOutcome` 旧档问题同一份长寿档）。
- 同根因另两面：双肩包档 4→3（且 `hasBag` 残留 → 凭空多出"丢下帆布袋"）、书包档 5→3；`换包` 正文 `if(vars.hasBag)` 会读到自己没有的帆布袋描写。
- 与 MEMORY §1 既定规则冲突：「改字段含义/结构应 bump `SAVE_VERSION` 弃档」——本次正是"字段含义变了"。

**新增工具**：`tools/bag_migration_repro.js`（无头真实引擎，9 ok / 6 BUG，精确复刻实测：迁移后 `bagVolume=3`→捡不了、丢袋后 `_bagExtra=-1`、再丢车钥匙后 `itemCount 2 < bagVolume 2` 仍不行）。
**报告**：`tools/背包容量旧存档迁移缺陷报告.md`（含 A/B/C 三方案 + 现有档的 console 临时解封）。
**顺手修掉一条假绿**：`bag_volume_selftest.js` 第 10 组 `ok(..., s.bagVolume >= 4 || true)` —— `|| true` 让断言恒真、注释却写"见下方说明"（下方无说明）。已改为「⚠ 已知缺陷，不计入通过数」并结尾单列，套件不再谎报（仍 32/0）。
**方案未动代码**，等波波拍板（A 补反推迁移 / B 只加 `_caps` min:0 / C bump 弃档）。

### 顺带发现（未动）
无头骨架（`mercury_engine_e2e.js` 等）里 `fatigueTier(_travelMinutes)` 求解失败（21 处"表达式错误"）⇒ **疲劳规则的 `triggerKey` 在所有无头测试中实际未生效**。`mercury_leak_guard.js` 用"engine.js 之后再 bind 一次 globalThis"绕开了，新脚本已照抄该写法（先 load engine、再 bind、再静音 console）。

---

## 收尾三件（波波点名"做 1、3、4"）

### 1. 人物档案补 017/018/019（蔡镜晓 / 彭奕宸 / 刘冠宇）
严格按代码已落地部分写（同 016 的口径），每节末尾留"未实装（留待补充）"清单给波波。
- **017 蔡镜晓**：弘渊楼 4F 电脑区（打明日方舟）常驻；饭点 11-12/17-19 去食堂后厨 → 搭话选项与"躲起来"选项一起消失（躲藏文案依赖他在场）；搭话三态（dd<2 后厨有吃的+刘冠宇腿伤 / dd≥2 煤气阀任务线索 / 李娟提醒）；是 `建平-食堂-后厨` 食物的信息来源；复旦章 b 链骑车来援、抡自行车锁。
- **018 彭奕宸**：高三 14 班（玩家原班级）；修电脑（万用表）→ galgame 三结算（bad 不关门可重试 / normal +1 体力 / true +2 体力+隐藏 CG）；wqx 存档彩蛋（6/29 08:00）；《腐烂尸城》记忆；方便面；钢琴游走（13/16 点，圆厅/音乐教室/挹芬楼休息区，后者需 `_yifenEastCleared`）；`jpXinFuse` 软信号"物理办公室那边……最近别去。"
- **019 刘冠宇**：食堂腿伤（自述"被门夹"）；两条死路（`chasedByZombies>=3` → zombie / `dd>=3` 且未关阀 → gas），`_liuCorpse` 锁存；dd≥2 闻到臭鸡蛋味预警；关阀后道谢；复旦章电话一句（死人不给台词）。
- 头部同步：标题 001-016→001-019、编号说明、实装状态、总表 +3 行；016 未实装清单第 4 条改为指向 017-019。

### 3. `lint_story.mjs` 孤立场景假阳性（根治）
**根因**：入边记账靠 `mkVariants()` 那 7 组固定状态实走 `choices()`/`nextScene()`；函数式分支（如 `_xinKnowsTruth && !_studentsCalled`）不在变体组合里 → 其目标取不到 → 误报"无任何入边"。另一面：`areaFilter` 模式只加载单文件，跨文件入边（建平中学.js → 结局-变了的忻老师）必漏。
**修法**：新增「3.5 入边补账」pass —— 从场景 `qte`/`onEnter`/`choices`（函数或数组元素）的**函数源码里提取字符串字面量**，命中 `idSet` 者补记为入边；**全量跑、不受 areaFilter 影响**。方向性：只增不减 → 孤立检测偏宽松（可接受，该项本就是 W 级人工核对）。
**效果**：复旦江湾 `W 2 → 0`；全图仍报 **16 处真孤立**（未被掩盖），`E=0`。

### 4. `MEMORY.md` 精简（18.2k 字节 / ~4.8k 字符 → **2992 字符**）
- 去重合并：把"做法/过程/工具清单细节"下沉到 `tools/*.md` 与 skill，正文只留必须遵守的规则 + 抓手。
- 删掉「待拍板」整节（改指向每日日志）、删掉已被 skill 覆盖的沙箱细节、删掉与 skill 重复的审计说明。
- 保留全部核心铁律：变量来源/派生量/computed、三件套、`story_files` 唯一权威、存档迁移口径、战斗徒手化与失败结局改法、`combatCost` 数值、背包容量与拾取四件套、割锯工具优先级、遥测块位置、`weather` 与雨天三选一、`timeImage` 工厂、`_visit` 只读与键名、过夜两层门槛、QTE 双轨、shake 31 处、文风禁词、气味成因、汞系统全部（`_caps`/慢性规则/派生/体征走正文/禁止点明机制/减汞手段/泄露守卫）、复旦章全参数。

**回归（全绿）**：`condition_audit` 0（函数体 0 处）/ `scene_fn_selftest` 0 异常 / `lint_story` 全图 **E=0 W=109**、复旦江湾 **E=0 W=0** / `xin_chapter_check` **55/55** / `bridge_node_check` **22/22**。

**顺手修掉一条"不可复现"**：全图 W 一度在 109 / 110 之间跳。根因 = `story/` 里多处用 `Math.random()` 决定 text/nextScene（仁济南院-检验科撬门、新达汇-B1停车场接线、夜晚剧情-仁济摸黑、新达汇卡猫文案等），lint 每次运行命中不同分支 → W 计数 ±1 → **「绿」无法当回归基线**（这次差点被误判成"我的改动引入了新警告"）。
修法：在 `lint_story.mjs` 的 vm context 注入固定 `Math.random = () => 0.5`（落各阈值 false 侧）；随机分支的另一个目标已由「3.5 入边补账」的字面量扫描补齐，**不会因此漏记入边**。
验证：连跑 3 次全图 **E=0 W=109**、复旦江湾 **E=0 W=0**，逐次一致。

---

## 整理整理「非丢弃选项独立节点」盘点 + 补齐（波波点题）

**结论**：`story/core.js` 的「整理整理」主节点（:626）共 **23 个非丢弃（使用/查看类）选项**，其中 22 个原本就有独立 `整理整理-*` 节点，**唯独「用医用酒精消毒伤口」没有** —— 效果内联在选项 `effect` 上、直接 `nextScene:"整理整理"`，是全清单里唯一"有实际效果却零描述"的选项。
- **补齐**：新增 `整理整理-用酒精消毒`（紧邻 `整理整理-碘伏消毒`）。onEnter **原样承接**内联逻辑（`hurtByZombie=false` / `hasAlcohol=false` / `itemCount-1` / `updateTime(1)`），text 补写消毒过程 + 与碘伏同款系统提示；主节点该选项 effect 撤掉、`nextScene` 改指新节点。**效果零变化**（同耗时/同清伤/同丢瓶），纯"补描述 + 结构对齐"，未动任何数值。
- 旁支已核实、无需处理：`用手机看看有什么消息` → `长者食堂-手机信息`（定义在 `东明街道/长者食堂.js:399`）；`丢一盒碘伏棉签` 属丢弃类，不计入。

**顺手修掉一条既有工具坑**：`tools/mercury_engine_e2e.js` 每次回归刷 30+ 条 `fatigueTier is not defined`。根因 = 该脚本 sandbox **传了外层 Node 的 `Function`**（:43），而 engine.js 的 `evaluateExpr` 用 `new Function(...)` 求值 → 作用域落到 Node 外层 → 脚本自己 :78-90 那段"把 utils 顶层函数 BIND 到 globalThis"的补丁**完全失效**。修法：从 sandbox 移除 `Function`（留给 vm 自己的）。验证：假报 **0 条**、仍 **20/20**。（正是 skill `shichao-story-variable-audit` 坑表里那条"无头脚本不要给 sandbox 传外层 Function"——脚本踩了自己。）

**回归（全绿）**：condition_audit 两个 0 / scene_fn_selftest 0 异常（1438 场景）/ bridge_node_check 22/22 / save_compat_selftest 27/27 / unarmed 34/34 / bag_volume 32+1⚠旧档迁移（既有）/ bottle 72/72 / mercury_selftest 37/37 / mercury_engine_e2e 20/20（噪声已清）/ mercury_leak_guard 0 P0 / xin_chapter_check 55/55 / overnight B/C 段 0/0 / sprint_away 0 P0 / stamina_report 22 / stamina_telemetry 9/9 / qte_report、combat_penalty_report 正常重生成。文风扫描：新文案唯一命中 = 与碘伏同款的系统提示句（**有意复用**，同"体力+1"句式惯例）。

---

## 仁济南院 · 记忆闪色战斗失败后果全改死亡（波波点题）

**先验证前提**：波波说"4 处"，实际 **5 处**。他记的 4 来自 `tools/战斗惩罚审计报告.md` 正文那句"仁济南院（4 处）"，而**同一报告的明细表其实列了 5 行**——报告自身不一致（正文硬编码 vs 表格实算）。已向他确认：**5 处全改 + 每处写专属结局**。

**落地（`story/仁济南院.js`）**：

| 战斗节点 | 原失败节点（受伤续玩） | 新死亡结局 |
|---|---|---|
| 门诊大门-记忆闪色 | -记忆闪色-失败（−2 体力/+10 汞） | `结局-仁济-正门失守` |
| 急诊大厅-战斗 | -急诊大厅-受伤 | `结局-仁济-急诊失守` |
| 检验科-守卫战 | -守卫战-受伤 | `结局-仁济-检验科被咬` |
| 住院部走廊-战斗 | -住院部走廊-受伤 | `结局-仁济-病区失守` |
| 太平间-黑皮丧尸 | -黑皮丧尸-受伤（−3/+15） | `结局-仁济-黑皮丧尸` |

做法：每个战斗的 `elseScene` + `timeoutScene`（输错/超时两条）改指新结局；**原 5 个失败节点整体删除**（已 grep 确认无外部引用；`_renji*Cleared` / `_morgueCleared` 标记仅在胜利路径保留，未动）。新结局沿用既有 `结局-仁济-XXX` 形状：红字 `style`、`—— 结局：X ——` 收尾、无 choices。死亡一律不再结算体力/汞（项目惯例：死亡结局 onEnter 全空）。

**踩到的头号坑（已写进 skill）**：初版新节点叫 `结局-仁济-检验科失守`，与 `story/夜晚剧情.js:661` **已有同名结局撞车**（那是"过夜门锁被砸坏被咬死"，语义相近但完全不同）。`Object.assign(storyData, ...)` 聚合并无冲突检查 ⇒ 后加载者**静默覆盖**前者，而 `condition_audit`/`scene_fn_selftest`/`lint_story` 全在**合并后**的 storyData 上工作 ⇒ **全绿、无报错、真的丢了一个场景**。改名 `结局-仁济-检验科被咬` 规避。

**顺手沉淀 `tools/scene_id_dup_scan.js`**（本次手工 grep 的自动化）：运行时**逐文件隔离加载**（每文件独立 vm context，先预载 utils+core）比对 key 增量 + 文本正则通道给行号。⚠ 首版只做文本正则，漏了 **64 个循环/工厂生成**的场景（`建平-躲藏-*`、`三林安居苑-7号楼-*`、`张江-落地坡道` 等，源码里不是 `"ID": {` 字面量），是**假绿**——已升级为运行时通道，现覆盖 1437/1437，重名 0。

**报告脚本去硬编码**：`combat_penalty_report.js` 有 3 处写死的结论（"仁济南院（4 处）为受伤续玩"、"颜色 QTE 在仁济是受伤 −2 / 在建平张江是直接死亡"、"扫描 24 个文件"）——改完剧情后**报告会继续输出错话**。已全部改为动态统计：新增 `fileOf`（场景→定义文件）映射，按 A 类文件分组算死亡/受伤，编号文案随之自适应。

**旧档安全性（未 bump SAVE_VERSION）**：删场景节点是安全的——`engine.js:1396` 存档场景不存在 → **弃档**；`:1401` 历史栈里的已删场景 → **逐项过滤**。本次无字段含义变更，不需 bump。

**回归（全绿）**：condition_audit 0/0 · scene_fn_selftest 0 异常（1437 场景）· lint_story **E=0 W=113**（+5 = 新结局节点的"text 含空行"，与全部既有结局同款格式）· scene_id_dup_scan 重名 0 · save_compat 27/27 · unarmed 34/34 · bag_volume 32 + 1⚠（既有旧档迁移，待拍板）· bottle 72/72 · mercury_selftest 37/37 · mercury_engine_e2e 20/20 · mercury_leak_guard 0 P0 · bridge_node_check 22/22 · xin_chapter_check 55/55 · overnight B/C 段 0/0 · sprint_away 0 P0 · stamina_report 22 · stamina_telemetry 9/9 · combat_penalty_report **死 49 / 伤 17**（原 44 / 22，正好 ±5）。

## 09-24 玩家视角试玩·建平线（WorkBuddy 审玩）
- 自拟配置通关两轮（斧头+水瓶+饼干 / 探图流），模拟跳转逻辑、忽略 QTE 限时。
- 发现待拍板 P1：_xinOfferDay 在"当天上午够格但没上车"时记为当天，但办公室 hh≥14 分支与辅路文本都承诺"明天上午等你"→ 次日清晨 _xinGone 直接锁存，文本与标记互相打架（玩家会被文本坑死）。改 flag（过期后重录为 dd+1）或改文本（"过午就走"），需波波拍板。
- 孤儿节点：建平-挹芬楼-2F-高一教室-看看时钟 无任何入口。
- 设计风险：ch≥3 进物理办公室害死忻老师 / 进食堂害死刘冠宇——机制成立但无前置 diegetic 预警，首撞会觉得不公平。
- 文风：建平"————"四连破折号 10 处（张江基准仅 1 处）；"找王某和陈某"占位名出戏；3F/4F 机房文本是光杆占位。
- 火焚 Harsh 实际 +3 ch（选择 effect +2 与 火焚 onEnter +1 叠加），读代码易误判，建议注释或合并。
- 亮点确认：李娟借书证揭示、Harsh 内胆驱赶、galgame-wqx 存档彩蛋、橘猫三合一功能。

## 09-24 建平线审玩修复（按波波拍板执行）
1. 忻老师窗口改「错过即走」：_xinOfferDay 永远记许诺日当天（删 dd+1 分支）；办公室+辅路 onEnter 双记账（===0 先到先得）+ 同一套落定规则（跨日 或 当天 hh≥14 → _xinGone）。删办公室「明天上午等你」死分支（hh≥14 时 onEnter 已先落定，该分支不可达）；辅路/办公室 _xinGone 文案分「当天目送开车走/次日空位空屋」两态。core.js 注释同步。
2. 孤儿节点 建平-挹芬楼-2F-高一教室-看看时钟 补入口（高一教室 choices 加「看看黑板边挂着的时钟」，无时耗）。
4. 火焚 Harsh 删 onEnter +1ch，只留选项 effect +2（实际总惩罚 +3→+2）。
6. _yifenEastCleared 全量换 _visit['建平-挹芬楼-1F-东侧走廊-清场']（对齐西侧走廊写法）：建平中学.js 4 处读 + effect 清空、core.js 默认值删除 + 钢琴游走触发器改读 _visit。旧存档等价：该变量为 true 时必经清场节点，_visit 键必在，无需迁移。
- tools/_core_before.js 是无引用的历史快照，未同步（故意）。
- 3（ch≥3 害死忻/刘 的 diegetic 预警）与 5（文风占位）波波自己处理，未动。
- 回归：condition_audit 0/0、scene_fn_selftest 0 异常、sprint_away 0 P0、xin_chapter_check 65/0（新增 7b 组 10 项断言覆盖错过即走）、save_compat 27/0、bag 32/0、bottle 72/0、unarmed 34/0、mercury 37/0、bridge 22/0、chain/area/lint/graph/dup_scan 全绿。

## 09-24 忻老师窗口·方案A落地（波波拍板：严格错过即走）
- 分析发现洞：记账原锚在「首次进办公室/辅路」，14 点后清开门 + 回避两场景撑过强制过夜 → 次日记账得完整上午窗口（重置漏洞）。
- 修复：三处开门点（后门-开门 / 内侧-开门 / 开门-清场后）onEnter 加记账 `if (!_xinDead && !_teacherLeft && _xinOfferDay===0) _xinOfferDay=dd`；办公室/辅路记账降级为兜底（注释已标）。落定规则不变（跨日 或 当天 hh≥14）。
- xin_chapter_check 新增 7c 组 9 断言（开门记账、过夜重置已堵、当天目送、清场后开门点）。全套回归绿：xin 74/0、condition 0/0、scene_fn 0 异常、save_compat 27/0、bag 32/0、bottle 72/0、unarmed 34/0、mercury 37/0、bridge 22/0、lint E=0、e2e 0 异常。
- 已知设计后果（已拍板接受）：Day1 清开门、Day3 才初见忻 → 直接空屋；接任务台词未提「两点」的公平性问题归 ③ 待办。
