# 建平中学.js 体验反馈

> 评审日期：2026-09-18
> 评审范围：`story/建平中学.js` 全文（含垂直通道工厂、Harsh 追踪包装器、夜间过夜衔接）
> 评审角度：玩家实际游玩体验（跳转逻辑 / 状态一致性 / 文本质量 / 规范符合度）
> 图片资源不在本次评审范围（按用户要求跳过），但涉及**图片路径写错**的会单独标注。

---

## 一、必须修的跳转 / 状态 bug

### 1. 【严重】挹芬楼楼梯间：楼层解析失败 → 跳到不存在的场景，玩家硬卡死

- 位置：`jpStairChoices` 保底分支（约 24-28 行）+ `建平-挹芬楼-东楼梯/西楼梯`（1074-1075 行）
- 现象：`jpCurrentFloor` 靠 `_lastScene` 末位 `(\d+)F` 反推楼层。以下两种常见情况都会解析失败，走"列出全部楼层"的保底分支：
  1. **从挹芬楼 1 楼走廊进楼梯间**（`_lastScene = "建平-挹芬楼-1F-东侧走廊"`，以"走廊"结尾，不匹配 `NF`）——这是最常走的路线；
  2. 清掉堵路丧尸后 `nextScene: sceneId` 跳回自身（`_lastScene` 变成楼梯间自己的 ID）。
- 后果：保底分支生成 `"去1楼" → nextScene: "建平-挹芬楼-1F"`，而**挹芬楼 1F 被拆成了东西两条走廊，没有 `建平-挹芬楼-1F` 这个场景**。点下去直接 `【错误：找不到场景 建平-挹芬楼-1F】`，选项区被清空，只能靠右上角"回溯"自救。
- 附带的次生问题：保底分支同时**丢掉了"只能上下 1 层"的限制**，玩家在楼梯间看到 6 个楼层选项，可以 1 楼直达 6 楼，绕开了电梯 = 唤醒了 Harsh 的成本设计。
- 修法建议：给 `jpStairChoices` 加 `floorOverrides` 参数（照抄 `jpElevator` 的做法，`1 → 建平-挹芬楼-1F-西侧走廊`）；堵路丧尸清场后不要 `nextScene: sceneId` 自跳，改跳"上一层的走廊"或加一个 `_stairKillNote` 展示用的中转节点。

### 2. 【严重】食堂后厨：进入第 5 次 = 毫无预警的即死

- 位置：`建平-食堂-后厨` onEnter（1788-1790 行）+ `core.js` 全局触发器（547 行 `gasIndex >= 100 → 结局-煤气中毒`）
- 现象：每次进后厨 `gasIndex += 20`（上限 100）。第 3、4 次会以 QTE 形式给压力（15s / 10s），超时只是被赶出后厨（`建平-食堂-后厨-窒息`，**不死**）。但第 5 次 gasIndex 顶到 100，全局触发器在 onEnter 之后立即执行，**玩家连 QTE 都看不到就直接进"结局-煤气中毒"**。
- 后果：玩家在"关阀 → 找食物 → 再进后厨确认"的探索节奏里会莫名暴毙；而且与"窒息节点不致死"的设计意图自相矛盾（玩家会认为后厨是可反复进出的危险区，不是即死区）。
- 修法建议：三选一——① 全局触发器条件加 `&& !_gasValveClosed` 并让 100 走 `建平-食堂-后厨-窒息`；② `gasIndex` 单次增量递减（如 20/15/10/5）或封顶 80；③ 在 60/80 档的 text 里明确"再进来就出不去了"。

### 3. 【中】手枪最后一发变必死

- 位置：`jpBlockedStair` 的枪选项（69 行）
- 现象：`effect` 里先扣子弹，`nextScene` 再判断 `v.gunAmmo > 0`。引擎是**先执行 effect、后解析 nextScene**（`engine.js:724-726`）。所以带着**正好 1 发**子弹时：扣完变 0 → 判定为"空枪" → 跳 `建平-结局-空枪` 死亡；而此刻 `_stairKillNote` 和 `clearedVar` 都已经写好了，等于"开枪打死了丧尸，然后因为没子弹被咬死"。
- 对比：`建平-后门-开门` 的枪选项把判断放在 `nextScene` 里、扣子弹放在目标场景 onEnter，写法是对的。同一文件两套写法。
- 注：同样的写法在 `东明街道/长者食堂.js:149`、`金谊广场.js:337/502`、`全家和公交站.js:525` 也存在，建议一并修。
- 修法建议：统一成"nextScene 判断用 `_pendingShot` 之类的临时标记"，或把扣弹挪进目标场景 onEnter。

### 4. 【中】老吴"诈尸"会在已经搜过尸体后触发，并且能刷 `itemCount`

- 位置：`建平-致真楼-1F-老吴杂物室-查看老吴`（1229-1244 行）
- 现象：诈尸分支条件是 `dd >= 3 && !_laowuKilled`。但玩家在 Day 1-2 就能在杂物室看到"老吴趴在地上，早已没了气息"，并**搜尸体**拿走钥匙串 + 管线图（`-搜尸体` 只置 `hasKeyRing/hasPipelineMap`，不动 `_laowuKilled`）。到了 Day 3 再回来看他，**已经确认死亡并翻过身的尸体又诈尸了**。
- 附带：`-击杀`（1287 行）无条件 `add: { itemCount: 1 }`，与 `-搜尸体`、`-抢管线图` 三处都会 +1，重复走会虚增 `itemCount`（`hasXxx` 已是 true，物品只有一份）。
- 修法建议：诈尸守卫改成 `dd >= 3 && !_laowuKilled && !hasKeyRing`（搜过就当已经确认死亡），或加一个 `_laowuCorpseSeen` 标记；三处的 `itemCount` 改成 `condition` 判断或先置 `_laowuLooted` 守卫。

### 5. 【中】Harsh 追踪包装器的排除名单漏了 4 个非地点节点

- 位置：`NON_PLACE` 正则（3006 行）
- 漏项（都会被打上"地点节点"标签、记进 `_harshTrack`，污染距离判定）：
  | 场景 ID | 原因 |
  | --- | --- |
  | `建平-结局-空枪` | 以 `建平-` 开头、不匹配 `EXCLUDE`（该正则的 `结局-` 是行首锚定），`NON_PLACE` 里也没有"空枪"——**一个结局节点被当成地点记进轨迹** |
  | `建平-崮山路-阀门箱-锁着` | 名单里只有"锁门" |
  | `建平-远翔楼-3F-物理办公室-没钥匙` | 名单里没有"没钥匙" |
  | `建平-济美楼-4F-音乐教室-听音乐` | 名单里是"听琴"，不是"听音乐" |
- 修法建议：`NON_PLACE` 补 `空枪|锁着|没钥匙|听音乐`，并把 `建平-结局-` 加进 `EXCLUDE` 更稳妥。

### 6. 【中】音乐教室：彭奕宸不在场，却能从"听音乐"里听到他弹琴

- 位置：`建平-济美楼-4F-音乐教室`（2140-2162 行）
- 现象：默认分支明确写"一架旧钢琴蒙着灰……**空无一人**"，但选项"休息一下，听音乐"永久可见，目标场景 `-听音乐` 永远是"彭奕宸十指在琴键上轻轻起落，一首《命运交响曲》从指尖流出"。
- 修法建议：选项加 `showCondition: function(v){ return jpPengAtPiano(v, 3) || v._pengGalCleared; }`，或让 `-听音乐` 的 text 按在场与否分流。

### 7. 【轻】结局节点的武器耐久处理不一致

- `结局-后门失守` / `结局-被老吴咬死` / `结局-宿舍失守` 都挂了 `tryBreakWeapon`，但同样是"闪色失败"的 `结局-前门失守`、`结局-挹芬楼失守` 没挂。按 CLAUDE.md「武器耐久」第 1、3 条应统一。

### 8. 【轻】"内胆"场景有重复句

- 位置：`建平-废弃小楼-3F-团委工作室-内胆`（2241-2247 行）
- `if (vars._harshActive)` 里追加了"你抽出那件校服外套的内胆——软软的，还带着点霉味。"，后面又**无条件**拼了一句一模一样的话。玩家会连着读到两遍。
- 顺带：该段说"想到**楼上**那声凄厉的嚎叫"——Harsh 是在电梯井/挹芬楼被唤醒的，玩家此刻站在废弃小楼 3 楼，方位对不上。

### 9. 【轻】图片路径写错（不是"图还没做"，是路径指向了别的区域）

- `建平-前门`（305-317 行）四个分支都指向 `images/仁济南院/前门.webp`、`前门-清场.webp` 等。**仁济南院目录下根本没有"前门"这套图**（那边叫"门诊大门"），而 `images/建平/` 下正好有现成的 `前门.jpg / 前门-night.jpg / 前门-清场.jpg / 前门-清场-night.jpg`。所以这里会显示裂图。同时注释里的 TODO 路径写的是 `images/jianping/xxx.png`（英文目录），与实际资产目录 `images/建平/` 也不一致——建议把 TODO 路径统一改成 `images/建平/`。

---

## 二、主线终点：复旦江湾是"剧终 + 开发备注"

- `建平-后门辅路` →"跟忻老师上车（去复旦）"→ `建平-前往复旦` → `复旦江湾`，而 `复旦江湾` 无 choices、无 qte → 引擎直接渲染 **"— 剧 终 —"** + 回溯按钮。
- 它的正文里还留着：`（复旦江湾 · 王知筠实验室剧情尚未实装）`——**开发待办直接印在了玩家的通关画面上**，这是最伤体验的一处。至少要把括号删掉，并把它写成一个有名有姓的结局节点（例如 `—— 结局：驶向江湾 ——`），或者留一句自然的"未完待续"。
- 另外：这个选项会静默清空玩家所有交通工具（`hasCar/hasEbike/hasRustyBike/hasScooter` 全置 false），选项文字里没有任何提示。建议文案补一句"（车留在学校，之后步行）"，或在目标场景补一句交代。
- 衔接检查：`复旦江湾` 的 `currentArea = "复旦"`，但 `夜晚剧情.js` 的兜底过夜分支排除名单里只有 `仁济南院/建平中学/张江`，**没有"复旦"**。虽然当前它是终点节点不会触发，但一旦后续把复旦做开（要能来回走），必须补过夜分支，否则玩家会被"街头过夜"传回初始小区。

---

## 三、引导 / 难度节奏

### 10. 主线必经步骤被起了个"自杀"名字

救忻老师这条主线，玩家必须先在 `建平-后门` 选 **"作死打开后门"**。虽然物理办公室的对话说了"你去后门看看"，但选项字面写着"作死"，玩家（尤其胆小/谨慎型）会本能避开，整条主线就此断掉。建议改成"拉开后门，引开丧尸"之类的动作描述，把风险写进目标场景的文本里，而不是写在选项上赶玩家走。

### 11. 反复路过的枢纽节点每次都 +1 追兵，雨天尤其致命

`建平-金苹果广场`、`建平-水池`、`建平-宿舍-门口`、`建平-弘渊楼-1F`、`建平-济美楼-1F`、`建平-废弃小楼-1F`、`建平-行政楼-1F` 都是"onEnter 即 `add chasedByZombies: 1`"。晴天会因为户外归零而消解，但**雨天不归零**：一个想去食堂/宿舍的玩家来回走两趟，ch 就顶上 3-5，走廊 QTE 被压到 10 秒内，还没见到内容就先被围殴。
建议：给这类"已经被玩家清过/走过一遍"的枢纽节点加"首次进入才 +ch"或"两次进入之间至少间隔 N 分钟才再 +ch"，至少在雨天做衰减。

### 12. 楼梯间本身就是惩罚区

`建平-致真楼-西楼梯`、`建平-远翔楼-东楼梯` 每次进入 `chasedByZombies + 1`。这两栋楼没有电梯，玩家上 4 楼要爬 4 次楼梯 = 必然 ch 4（且 QTE 挂在楼层节点上，一上去就是 12 秒限时）。建议楼梯的 +ch 改成"每层交替不加"或"仅在 ch==0 时 +1"，避免无电梯楼变成纯受罚动线。

### 13. 反复坐电梯可以无限重置 Harsh

`jpElevator` onEnter 每次都会 `_harshLag = 6`、`_harshTrack = []`。也就是说只要玩家在电梯里刷一下，Harsh 永远追不上——威胁形同虚设；而且每次都会重复弹同一条"有什么东西醒了"的 toast（她明明早就醒了）。建议：仅在 `!_harshActive` 时重置与提示；已激活时只做"重新落后 2 步"之类的小补偿。

### 14. "继续休息"是负收益

`建平-宿舍-内部-休息` 的"继续休息"只 `updateTime(120)`，**没有 `restRecover`**。两小时按饥饿规则大概率 -1 体力 + 不回体力 = 玩家点了"休息"结果更累。而且它的目标文本写"再次醒来时，天色已经变了"——宿舍里看不到天，且 2 小时也未必变天（除非跨 19:00 被夜间系统接走）。
建议：给这条加 `restRecover(vars, 1)`，或明确改成"睡到晚上"这类有实际语义的行动。

---

## 四、文本：唐突 / AI 味的段落

### 15. 操场踢球后，地点描述整个换掉了

`建平-操场` 在 `_playgroundKicked` 为 true 之后，正文从"这里是操场……靠近球门的地方滚着一只孤零零的足球"整段替换成"远处高耸的松树和低矮的草丛，在视野尽头静静地立着"。玩家只是踢了个球，回来发现操场变成了别的地方（而且新描述里没有操场）。
建议：改成"球门里那只球还躺着"之类的承接写法。
顺带说：这一段踢球回忆（中圈试射、班主任"别踢了回教室刷题"）是全文件写得最好的段落之一，正因为如此，后面的替换句落差格外明显。

### 16. 阀门箱里那段"真相总结"——最典型的 AI 腔

`建平-崮山路-阀门箱-查看` 在玩家已有管线图时，直接追加：

> 老吴不是尝出来的——他修了十七年水管，是从直饮水里带出的泥沙和那股说不上来的不对劲，才一路追到这口井里的。真正害人的东西无色无味，他没能带走那个水样。

问题有三：① 玩家此前只见过图纸上"水有毒，别喝"六个字，这段等于替玩家把推理结论和人物前史一次性总结出来，**本该靠线索拼装的悬念被旁白说破了**；② 它被包在 `<span style='color:#ffaa00;'>` 警告色里，但它不是警告，是解说，颜色用错；③ 语序和用词都很"总结陈词"（"真正害人的东西无色无味"），与全文克制的观察式叙述完全不同。
建议：删掉这段解说，改成一条可观察的物证（比如采样瓶里残留的沉淀物、老吴写在箱门内侧的一行日期），把结论留给玩家自己拼。

### 17. 电脑区两人对话的指代错乱

`建平-弘渊楼-4F-电脑区` 在校友 galgame 通关后追加：

> 彭奕宸也占了旁边一台电脑，玩得正起劲——这俩家伙，一个图书馆一个教室，满学校乱窜。
> "这都什么时候了，你俩还搁这打舟呢？"蔡镜晓说……

问题：① 彭奕宸**此刻就坐在电脑区**，"一个图书馆一个教室"不成立（他人在"图书馆"，蔡镜晓也在"图书馆"，哪来的"一个教室"）；② 蔡镜晓说的"你俩"——如果指玩家和彭奕宸，那蔡镜晓是在对玩家喊话，但他自己也在打游戏，逻辑上他在骂自己；③ "这俩家伙"是旁白口吻，紧接蔡镜晓的引号台词，叙述层次糊在一起。
建议：拆成两句，明确"蔡镜晓抬头看了你俩一眼"之类的动作归属。

### 18. 彭奕宸在 13:00 分身三处

`jpIsMealTime` 的午餐窗口是 11-13（**含 13 点整**），而 `peng-piano` 规则在 `hh === 13` 时也会 roll 一次 `_pengPiano`。于是 13:00 这一个小时里，玩家可以在**高三 14 班**（"正坐在自己的座位上，掏出一包方便面"）、**远翔楼圆厅**（"正坐着，一下一下地按着琴键"）、**济美楼音乐教室**（"正坐在钢琴前"）三处同时看到彭奕宸。建议把午餐窗口收成 11-12，或把钢琴时段挪到 13:30 之后。

### 19. 时间尺度违规 + 时间线穿越

- `建平-远翔楼-5F-自习室`：**"这层楼像是被废弃了很久"**。危机才第 1-5 天，按 CLAUDE.md「环境叙事时间尺度」，不能用"废弃了很久"。同段的"成箱的废纸、坏掉的黑板"也偏重。建议改成数日尺度的描述（"桌椅落着薄灰、旧卷子被风掀得到处都是"）。
- `建平-挹芬楼-5F-高二教室-黑板`：写着 **"Day 2 · 还剩 11 个人"**。玩家完全可以在 Day 1 就走进这间教室，看到一句属于"明天"的留言。建议加 `dd >= 2` 门控，或改成不写天数的写法。
- `建平-济美楼-1F` 的"回字形走廊延申到屋顶"——"延申"应为"延伸"（错别字）。

### 20. 其他唐突处（轻）

| 位置 | 问题 |
| --- | --- |
| `建平-宿舍-内部-发现狼人杀手牌` | "继续休息"（+120min）后突然摸出"金色镶边、红眼狼人、黑毛飘飘、张牙舞爪"的狼人杀卡牌——与主线毫无关联，不加记忆、不给 flag、没有任何后续，描写风格也和全文不一致；同时"你用力撑起身子，感觉摸到了什么"这种句式很像 AI 硬转场。建议要么给它一个去处（记忆/后续 NPC 反应），要么砍掉。 |
| `建平-前门`、"查看旁边"（`建平-校园门口` 226 行） | 选项名太模糊，玩家不知道会去哪。按规范开放式场景应使用明确的地点/绝对方位表述，建议改成"看看路边那只井盖"。 |
| `建平-远翔楼-3F` | "地板上躺着一具尸体，不知道是哪个倒霉蛋。"——这具尸体不可交互、无任何线索，"倒霉蛋"的口吻也和主角克制的叙述不太搭。建议给它一个可查看选项（哪怕只是环境叙事），或删掉。 |
| `建平-门卫室` | 正文说有"墙上挂着全校班级的钥匙板"，但玩家在这里什么都拿不到，而主线又极度依赖"钥匙串"。玩家会立刻发问"钥匙板为什么不能拿"。建议补一句"钥匙板上的钥匙被人成串摘走了"。 |
| `建平-水池` | "丧尸像被什么吸引似的沿着池边越聚越多"，"越聚越多"是持续进行时，但场景是静态的、每次进只是 +1ch。建议改成静态描述。 |
| `建平-思贤堂` | "虽然看不到几只丧尸，但椅子的缝隙间应该躺着不少尸体"——"应该"是叙述者揣测，会削弱沉浸感。 |
| `jpHide` 生成的躲藏场景 | onEnter 里统一 `vars.showRain = true`。雨天躲进室内（宿舍、机房、团委工作室）屏幕仍在下雨。若非有意，建议按场景区分。 |
| `建平-橘猫-亲近` | 结尾"又停下回头"缺句号，读起来像被截断。 |
| `建平-弘渊楼-4F-电脑区` 的猫 | 橘猫只在辅路/大道/操场/远翔楼/挹芬楼 2F 出现，**中心枢纽金苹果广场反而没有**。玩家最可能先到广场，容易错过整条猫向导线。 |
| `建平-挹芬楼-6F-自习教室` 的夹心饼干 | 拿走的饼干只能回到这间教室才能吃（"吃掉夹心饼干"选项挂在这里），在别处想吃只能靠"整理整理"。要么把它并入通用的食物入口，要么文案说清"留着喂猫/回这儿吃"。 |

---

## 五、规范符合度（批量可修）

### 21. 9 处"🎒整理一下物品"入口都缺 `showCondition: "itemCount > 0"`

CLAUDE.md「整理整理自由入口」第 4 条要求自由入口统一带该条件（空背包不显示）。建平 9 处入口全部没有：

- `建平-校园门口`（225）、`建平-行政楼-天台`（852）、`建平-远翔楼-3F-物理办公室`（1505）、`建平-远翔楼-4F-高三14班`（1552）、`建平-食堂`（1756）、`建平-弘渊楼-4F-电脑区`（2047）、`建平-废弃小楼-3F-团委工作室`（2232）、`建平-挹芬楼-3F-机房`（2659）、`建平-挹芬楼-4F-机房`（2677）

对比：樱桃苑 5 处、新达汇 5 处、安盛街 4 处、安居苑 9 处、张江 4 处均已带该条件。这是目前**最容易被漏掉、也最容易批量修**的一类。

### 22. 对白引号混用（21 处 ASCII `"`，全项目最多）

CLAUDE.md 明确"text 文本里的引号必须用""。`建平中学.js` 有 **21 处**用了 `\"`，而其他 20 多个 story 文件合计只有 2 处（安盛街、东明街道路径各 1）。这正是"哪里像 AI 写的"最直观的痕迹——NPC 台词那几段（忻老师、蔡镜晓、彭奕宸、刘冠宇、学生）几乎全是直引号，而 galgame 那条线用的是中文引号，同一文件内部风格都不统一。

涉及行号：554、1192、1231、1262、1288、1490、1495、1497、1499、1570、1579、1770、1774、1776、1872、2070、2072、2074、2150、2740、2748。

### 23. 两件物品只占 1 格

`建平-致真楼-1F-老吴杂物室-搜尸体`（1260）与 `-击杀`（1287）都会同时给 `hasKeyRing` + `hasPipelineMap`，却只 `add: { itemCount: 1 }`。按「全图唯一物品 / 物品管理」的规范，两件东西应各占 1 格，否则背包容量系统在这里被绕过。`-抢管线图`（1296）只给管线图却也是 +1，是对齐的；前两处需要改成 +2 或明确"钥匙串不占格"（若是设定，应在系统提示里说明，像火把那样）。

### 24. 内胆没有系统提示、也不占格

`建平-废弃小楼-3F-团委工作室-收好内胆` 用 `add: { hasInnerLining: 1 }`（数值型而非布尔，与 `hasXxx` 惯例不同，能工作但容易被后来者误改成 `hasInnerLining = true`），且没有 `【系统提示】`。玩家在 Harsh 堵住前根本不知道自己身上有这件道具。建议补一条提示（对照火把那条写法）。

---

## 六、顺带发现的引擎/工具层问题（会影响建平，但不是建平的问题）

1. **`updateTime` 的 `addMinutes` 是闭包内被改写的变量**（`story/utils.js:15-34`）。雨天时 `addMinutes = Math.round(addMinutes * 1.3)` 会**永久污染这个闭包**。凡是把 `updateTime(N)` 写进**静态 choices 数组**的场景（建平几乎全是静态数组，比如 `建平-食堂` 的"去后厨"、各大楼层的"去XX"），同一个闭包会被反复调用：`N=1` 因为四舍五入侥幸不变，但 `N=2/3` 会变成 3→4→5→7…**雨天反复走同一段路，耗时越走越长、体力越掉越快**。
   修法：在闭包内用局部变量（`var mins = addMinutes; if (雨) mins = Math.round(mins*1.3);`）。
2. **`jpStairChoices` 的保底分支与 `jpElevator` 的 `floorOverrides` 机制不对齐**——即第 1 条 bug 的根因，两处最好统一成同一个"楼层→场景 ID"映射工具。
3. CLAUDE.md 第 677 行写"'结局-煤气中毒'是仁济太平间外的 gasIndex 全局触发器目标"，但 `仁济南院.js` 里 grep 不到任何 `gasIndex/煤气`，该节点由 `建平中学.js` 独占定义、正文写的是"后厨"。文档这句已过时，建议顺手更正（同时它解释了为什么建平后厨的煤气是全局致死，见第 2 条）。

---

## 附：本次用到的自动校验

`tools/check_jianping.js`（临时脚本，可删）会在 Node 里加载全部剧情文件，然后逐个场景：

- 静态 + 函数型的 `nextScene / elseScene / timeoutScene / onTimeout` 目标是否存在；
- 所有 `condition / showCondition` 字符串能否正常求值（专抓"变量没在 `_variables` 注册"）；
- 列出建平的无选项节点（= 剧终）与 text 为空的场景。

本次结果：**条件表达式 0 报错**（变量注册很完整，这块没问题）；**跳转目标 2 处不存在**，都是挹芬楼楼梯间的 `建平-挹芬楼-1F`；`复旦江湾` 与 `建平-结局-空枪` 是建平线上仅有的两个"剧终"节点。
