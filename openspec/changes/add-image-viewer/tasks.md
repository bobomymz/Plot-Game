# Tasks

## 1. 查看器骨架（index.html + style.css）

- [ ] 1.1 index.html 在 `#image-area` 内新增 `#zoom-badge` 元素（默认隐藏）、在 body 末尾新增 `#image-viewer`（背板 + `<img>`，默认隐藏），验证：DOM 顺序不破坏 `#text-area ~ #choices-area` 兄弟选择器（查看器放最后），浏览器打开现有场景无报错、布局逐像素不变
- [ ] 1.2 style.css 新增 `#image-viewer` 样式：fixed 全屏深色背板、z-index 140、`touch-action: none`、`overscroll-behavior: contain`、内部 `<img>` contain 居中，验证：桌面 1280×800 与手机模拟 390×844 下手动显隐元素时遮盖范围正确（压过文本/状态栏，不压 QTE 条）
- [ ] 1.3 style.css 新增 `#zoom-badge` 样式：图片区右下角（桌面抬到文本卡 -200px 叠压区之上），手机端样式写进 `@media (max-width: 767px)` 内贴图片区右下角，验证：双端角标位置不被文本卡/状态栏遮挡
- [ ] 1.4 `cursor: zoom-in` 写进 `@media (hover: hover)`（挂 `#image-area` 的标记 class），验证：桌面标记场景悬停变放大镜光标，触屏无变化

## 2. 引擎交互（engine.js）

- [ ] 2.1 新增查看器状态模块（开关/缩放值/平移值均为引擎局部变量，不写 gameState）与 `openImageViewer()`/`closeImageViewer()`，打开时复制 `sceneImage.src`，验证：控制台手动调用两函数可开关查看器，图片与当前场景图一致
- [ ] 2.2 `renderScene` 按当前场景 `imageZoom` 字段切换 `#zoom-badge` 显隐与 `#image-area` 的标记 class（同 showRain 每场景重置惯例），验证：标记场景进/出，角标随场景正确出现/消失
- [ ] 2.3 `#scene-image`（或 `#image-area`）点击绑定：仅当前场景有 `imageZoom` 且图片可见时打开查看器，验证：未标记场景点击图片无任何反应，标记场景点击打开查看器
- [ ] 2.4 缩放与平移：Pointer Events 实现滚轮缩放（光标为中心，fit~4× 钳位）、双指捏合、放大后拖拽平移、位移阈值（~5px）区分单击/拖拽，验证：桌面滚轮放大可看清工牌文字、拖拽平移后松开不关闭；手机模拟捏合缩放正常
- [ ] 2.5 关闭途径：单击（未拖拽）、Esc（仅打开时监听）、`renderScene` 开头无条件 `closeImageViewer()`、`applyMemoryFlash` 开头调用 `closeImageViewer()`，验证：四种途径各测一次；打开查看器点击选项换场景后查看器不残留
- [ ] 2.6 QTE 协同验证（不改代码，仅验证层级与计时）：带场景级 qte 的场景打开查看器，QTE 条浮在查看器上继续走，超时照常跳转 `onTimeout` 且查看器自动关闭

## 3. 内容迁移（story 数据）

- [ ] 3.1 初始卧室（樱桃苑（初始小区）.js）加 `imageZoom: true`，首访文本末尾沿用灰色小字惯例补教学句（"带 🔍 的场景图可以点开细看"），验证：新局首访卧室文本含教学提示、图上有角标、点击可放大
- [ ] 3.2 找到利昂的工牌、库房的手机、高锦睿给的地图三张图所在场景，各加 `imageZoom: true`，验证：三个场景角标出现、点击可放大阅读图上文字
- [ ] 3.3 全库 grep `imageZoom` 确认仅预期场景带标记，验证：无遗漏、无误标

## 4. 双端回归与文档

- [ ] 4.1 桌面回归（1280×800）：未标记场景点击图片无反应、文本卡打字机跳过/展开收起不受影响、记忆闪色完整播放（若打开查看器时被触发则先自动关闭）、屏幕特效显示正常，验证：逐项手测通过
- [ ] 4.2 手机回归（Chrome 模拟 390×844）：三段布局不变、图片区无手势冲突（文本/选项滚动正常、下拉刷新仍被屏蔽）、查看器捏合/拖拽/单击关闭正常，验证：逐项手测通过
- [ ] 4.3 CLAUDE.md 同步：数据格式节加 `imageZoom` 字段说明、层叠顺序表加 140 行、"查什么→去哪"表加一行，验证：文档与实际行为一致
- [ ] 4.4 手机端适配.md 第九节补一条（查看器与角标的媒体查询归属、手势接管说明），验证：文档与实际行为一致
