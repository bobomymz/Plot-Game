# 手机端适配

本文档记录"让游戏同时适配电脑和手机"这轮改动的**设计决策与调参入口**。
具体数值以 `style.css` / `engine.js` 实际代码为准（本文档若与代码冲突，改本文档）。

## 一、目标与约束

| 项 | 结论 |
|---|---|
| 目标 | 手机竖屏**单屏紧凑布局**：图上 / 文中 / 选项下，一屏内完成阅读+选择，整页不滚动 |
| 硬约束 | **桌面端布局必须原样保留** —— 不是替换，是共存 |
| 本轮不做 | **图片一律不动**（不压缩、不懒加载、不改预加载逻辑）—— 实测手机加载速度可接受 |
| 顺带解决 | 手机误触返回/下拉刷新丢进度 → 加 localStorage 自动存档 |

### 双端共存的实现原则

1. **手机专属样式全部关在 `@media (max-width: 767px)` 里**，桌面看不到。
2. **基础层（无媒体查询）只允许三类改动**：
   - `dvh` 追加声明（`100vh` 后再写一行 `100dvh`，桌面两者等价，手机避免地址栏收放跳动）；
   - 纯新增规则（如 `touch-action`），桌面无感知；
   - 把 `:hover` 搬进 `@media (hover: hover)`（桌面行为不变，触屏不再粘滞高亮）。
3. **引擎里凡是写死"桌面形态"的内联样式必须移除**，否则 CSS 改不动它（详见 engine.js 改动）。

## 二、设备判定：三套标准，各管一件事

| 判定 | 位置 | 管什么 | 为什么不用另一个 |
|---|---|---|---|
| `@media (max-width: 767px)` | style.css | 布局（三段式、纵向选项、安全区） | 布局该跟着**可用宽度**变，桌面窗口拖窄也应该变紧凑 |
| `@media (hover: hover)` | style.css | 悬停高亮 | 触屏没有真 hover，按宽度判断会误伤窄窗口的鼠标用户 |
| `IS_TOUCH`（`(hover: none) and (pointer: coarse)`） | engine.js | 触屏输入补时 | 打字慢是**输入方式**决定的，与窗口宽度无关；桌面拖窄窗口不该放水 |

## 三、改动清单

### index.html（1 行）

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
```

- 加 `viewport-fit=cover`：刘海屏下 `env(safe-area-inset-*)` 才会返回真实值。
- 去掉 `maximum-scale=1.0, user-scalable=no`：禁用缩放是无障碍反模式，防误缩放交给 `touch-action`。
- ⚠️ DOM 顺序与 27 个 `<script>` 标签一律不动（`style.css` 靠 `#text-area ~ #choices-area` 兄弟选择器工作，换顺序会坏）。

### style.css

**基础层（影响双端，但桌面渲染逐像素不变）**

| 改动 | 作用 |
|---|---|
| `touch-action: manipulation`（选项按钮 / 重启 / 回溯 / 确认 / `#scene-text`） | 连点选项不触发双击缩放 |
| 4 处 `dvh` 追加声明（`html,body` / `#game-container` / `#image-area` / `.text-expanded`） | 手机地址栏收放时布局不跳 |
| `.choice-btn:hover`、`.choice-input-submit:hover`、`#backtrack-btn:hover` 移入文件末尾 `@media (hover: hover)` | 触屏点完不留高亮；原位置留注释指路 |
| `#qte-timer` 空规则 → 补全为真实样式块（含 `.qte-row/.qte-track/.qte-progress/.qte-countdown/.qte-urgent`） | 原样式写死在 engine.js 内联里，媒体查询管不到；迁移时保留 `margin-top: 8px` 以保证桌面像素一致 |
| 新增 `#save-dialog` 系列样式 | 存档确认弹窗；`z-index: 10000`，压过预加载遮罩的 9999 |

**`@media (max-width: 767px)`（手机专属）**

- 三段式：`#game-container` 竖向 flex + `100dvh`；`#image-area` `flex: 0 0 auto` + `aspect-ratio: 16 / 9` + `max-height: 42dvh`（2026-09-10 起，原为 `flex: 0 0 42%`，见第八节）；`#text-area` `flex: 1 1 auto` 内部滚动；`#choices-area` `flex: 0 0 auto` + `max-height: 38dvh` 内部滚动。
- flex 子项一律带 `min-height: 0`，否则内容撑破容器、外层被迫滚动。
- `#text-area` 清掉桌面的 `-200px` 负 margin 上叠和 `max-height: 100px`；`backdrop-filter` 14px → 8px（手机性能降档）。
- `#choices-area` 改 `flex-direction: column`，选项纵向堆叠；`.choice-btn` `min-height: 46px`（≥44px 触控目标）。
- `html, body { overflow: hidden; overscroll-behavior-y: contain; }`：整页不滚 + 屏蔽 Android 下拉刷新（误触即丢进度）。
- `#image-area` 加 `-webkit-touch-callout: none` + `user-select: none`（长按图片不弹系统菜单）；但 `.choice-input-field` 单独恢复 `user-select: text`，否则输入框不能选。
- 安全区：`#status-bar`、`#choices-area` 底部 padding、`#qte-timer`、展开态、`#status-warning` 全部用 `env(safe-area-inset-*)` 让位。
- `#qte-timer` 下移到 `安全区 + 64px`，不再压住右上角"重启 / 回溯"。
- 展开态用 **top + bottom 双锚点**（底部留 148px 给选项，选项区自身 `max-height: 132px`），保证展开的文字区和选项区不重叠。
- 丧尸包围遮罩（`zombie-surround-moderate/heavy` 的 `::after`）收缩到图片区高度（2026-09-10 起为 `min(56.25vw, 42dvh)`，原为固定 `42dvh`，与图片区实际高度保持一致），不再全屏延伸——全屏时它会盖在图片上、却被 z-index 60 的文本/选项盖住，只露出图片上下两条残影。暗角和雨滴维持全屏不变。

### engine.js

| 改动 | 说明 |
|---|---|
| 抽出 `startQteUi(timeoutMs)` | 原本场景级 / 选项级两处**逐字节相同**的内联样式代码，合并为一处，样式交给 CSS（顺带补了 `timeout` 被钳到 0 时的除零保护） |
| 删掉 2 处 `choicesArea.style.flexDirection = "row"` | 内联样式优先级压过媒体查询，不删则手机端选项永远横排 |
| `renderScene` 中新增内部滚动回顶 | 手机端文字区/选项区各自可滚，换场景要回到顶部 |
| 新增**触屏输入补时**（见下节） | 手机中文输入法慢，只放宽输入型选项 |
| 新增**自动存档模块**（见下节） | localStorage |

## 四、触屏输入补时（记忆闪色/输入型选项）

**问题**：选项级倒计时和闪色动画是**同时**启动的（`renderChoices` 先跑、`applyMemoryFlash` 紧随），所以 `timeout: 12000` 里前几秒是在看颜色闪。手机慢的只是"唤起键盘 → 拼音 → 选字"这段，不是看闪色。

**做法**：只把**扣掉闪色时长之后的敲字时间**乘系数。

```
新 timeout = 闪色时长 + (原 timeout - 闪色时长) × TOUCH_INPUT_FACTOR
```

- 生效条件：`IS_TOUCH && choice.input`。场景级 `qte`、纯反应型选项 QTE 一律不碰。
- `闪色时长 = 序列长度 × (MEM_FLASH_MS + MEM_PAUSE_MS)`，由 `memFlashDuration(vars)` 计算；闪色常量已提为 `MEM_FLASH_MS = 600` / `MEM_PAUSE_MS = 200`，与 `applyMemoryFlash` 共用同一份，避免两处漂移。
- **调参入口：`TOUCH_INPUT_FACTOR`（当前 `2`）**。想更狠改 `1.5~1.6`，设 `1` 即完全关闭该补时。
- **为什么不整条 ×2**：那会把"白给"的看闪色时间也翻倍，且颜色越多的关卡放水越狠 —— 正好抹平"颜色越多时限越紧"这条有意的难度曲线（见 `记忆闪色` 节与难度哲学）。

实测（Chrome 设备模拟）：

| 场景 | 桌面 | 手机 |
|---|---|---|
| 教程-识别颜色躲丧尸（5 色，原 12s） | 11.9s | 19.9s（4s 闪色 + 8s×2） |
| 家门外的丧尸（场景级纯反应 QTE，7s） | 6.7s | 6.7s（不变） |

## 五、自动存档（localStorage）

手机上误触返回手势 / 下拉刷新 / 后台被杀都会清空内存态，故加自动存档。

| 项 | 值 |
|---|---|
| key | `shichaobiji_save_v1`（`SAVE_KEY`） |
| 版本 | `SAVE_VERSION = 1`，不匹配即弃档当新局 |
| 存档内容 | `sceneId` / `gameState` / `historyStack`（末 `HISTORY_CAP = 30` 步）/ `reactiveState` / `savedAt` |
| 写入时机 | **只在 `renderScene` 末尾一处**调用 `saveGame(sceneId)` |
| 失败处理 | 首次读写抛异常（隐私模式/配额满）即 `storageOk = false`，警告一次后静默停用，不影响游玩 |

### 关键设计点（改动这块前必读）

1. **单一 hook 点是有意的**：全局触发器级联时外层 `renderScene` 会提前 `return`，只有最终落地那一层走到末尾 —— 所以"玩家每次动作恰好存一次"，选项点击 / QTE 超时 / 回溯 / 重启全部经此路径。
2. **`Set` 必须特殊序列化**：JSON 不支持 `Set`，用 `setReplacer` 转 `{ __set: [...] }`、`setReviver` 还原，`snapshotState()` 封装深拷贝。三个记忆集合（`gameMemorySet` / `personalMemorySet` / `mixedMemorySet`）靠它存活。
3. **`_reactiveState` 必须一起存**：规则节流键存在引擎全局、不在 `gameState` 里，不存会导致恢复后同一小时**二次扣体力**。
4. **`lastRenderedScene` 要从 `gameState._lastScene` 恢复**，不能直接赋 `saved.sceneId` —— 否则 `renderScene` 会把 `_lastScene` 写成"当前场景自己"，静默弄坏所有 `_lastScene` 承接句。
5. **遮罩 flag 要抢救**：恢复时 `skipOnEnter` 跳过了 `onEnter`，而 `renderScene` 会无条件重置 `showRain/showZombies/showPowerOut` —— `renderRestoredScene()` 负责前后夹住这三个值并补一次 `applyScreenEffects()`。
6. **弃档条件**：版本不符 / 缺 `gameState` / `sceneId` 已不存在于 `storyData`；`historyStack` 里指向已删场景的条目会被过滤（改剧情后老存档不会卡死）。
7. **重启按钮 = 清档**：`_reactiveState = {}` + `clearSave()`，随后 `renderScene` 立刻写入全新存档。
8. **恢复后不显示右上角"回溯"按钮**：正常游玩它常态隐藏（`pushHistory` 的显示语句被注释），回溯入口只有死亡屏。`applySave` 保持一致——历史栈照常入档/恢复，死亡屏照样可回溯，但顶部按钮不亮。

### 弹窗流程

启动时 `initGameState()` → `loadSave()`：有档则弹 `#save-dialog`（摘要形如 `Day 2 14:30 · 记忆 3 段`），两个按钮 —— "继续上次冒险"走 `applySave()` + `startPreload(sceneId, true)`；"从 Day 1 重新开始"走 `clearSave()` + `startPreload("start", false)`。无档直接开新局。

## 六、顺手修掉的 3 个老 bug

这轮为了做存档而引入 `snapshotState()`，正好治了三处 `Set` 相关的历史缺陷：

| bug | 原因 | 修法 |
|---|---|---|
| 回溯一次后记忆集合损坏（再 `.add()` 抛 TypeError） | `pushHistory` 用 `JSON.parse(JSON.stringify(...))`，`Set` 变成 `{}` | 改用 `snapshotState(gameState)` |
| "重新开始"后上一局记忆还在 | `initGameState` 用 `{ ...defaults }` 浅拷贝，与 `_variables` 共享同一批 `Set` 引用 | 改用 `snapshotState(defaults)` |
| 恢复/重启后同一小时二次扣体力 | `_reactiveState` 未纳入存档、重启也没清 | 存档带上 `reactiveState`；重启处 `_reactiveState = {}` |

## 七、已知取舍（有意不做）

- **恢复后 QTE 从满时长重新开始** —— 不记录倒计时残余，重开一局比"读档瞬间被判死"体验好。
- **刷新后回溯深度上限 30 步**（`HISTORY_CAP`）—— 内存中仍是全量，只是入档裁剪。
- ~~竖屏图片仍是 16:9 cover 裁切~~ —— 已于 2026-09-10 解决，见第八节。
- **iOS 无法用代码唤起键盘** —— 玩家必须自己点输入框，这部分耗时由 `TOUCH_INPUT_FACTOR` 一并覆盖。
- **图片压缩 / 懒加载 / 预加载超时兜底** —— 明确排除在本轮之外（预加载卡死无超时是已知问题，留待专门处理）。

## 八、手机端图片区改为 16:9 比例（2026-09-10）

**问题**：原 `#image-area` 固定 `flex: 0 0 42%`（约 1.1:1 的方框），而 63% 的场景图是 16:9（1.78:1），
`object-fit: cover` 在竖屏下把图片左右各裁掉约两成，"看起来像缺了一点"。

**改动**（都在 `@media (max-width: 767px)` 内，桌面逐像素不变）：

| 项 | 原 | 现 |
|---|---|---|
| `#image-area` 高度 | `flex: 0 0 42%`（屏高 42%） | `flex: 0 0 auto` + `aspect-ratio: 16 / 9` + `max-height: 42dvh` |
| `#scene-image` 填充 | `cover`（裁切铺满） | `contain`（完整显示，缺口补 `--canvas` 底色） |
| 丧尸包围遮罩 `::after` 高度 | `42dvh` | `min(56.25vw, 42dvh)`（始终等于图片区实际高度） |

**设计点：**

1. **图片区高度 = `min(屏宽 × 9/16, 42dvh)`**。普通竖屏手机（390×844）下为 220px，比原来 354px 矮，
   文字区反而更宽敞；矮宽屏（如折叠屏展开的小窗）被 `42dvh` 钳住，维持旧三段布局。
2. **选 `contain` 不选 `cover`**：图片宽高比实测分布为 16:9 附近 63%、中间比 17%、更宽 13%、方形/竖长 7%。
   区域钉死 16:9 后 `cover` 仍会裁那 37% 的图（方形图上下要裁掉 44%），`contain` 则任何比例都不丢内容，
   补边用深色 `--canvas`，与整体暗色风格一致。
3. `56.25vw` = `100vw × 9/16`，即图片区宽度推算高度；遮罩的 `background-size: 100% 100%` 会随之拉伸，无需另改。

**验证**（Chrome DevTools 模拟 390×844）：16:9 图完整无裁切；2.49:1 超宽图上下补黑边完整显示；
丧尸遮罩 `::after` 高度 219.6px 与图片区 220px 对齐；1280×800 桌面端图片区仍为 100dvh 全屏 `cover`。

## 九、后续改动注意

- 新增/修改任何 `:hover` 效果 → 写进文件末尾的 `@media (hover: hover)`，别写在基础层。
- 新增固定定位浮层 → 手机端记得用 `env(safe-area-inset-*)` 让位，并检查是否压住 `#status-bar`（右上角重启/回溯）或 `#qte-timer`。
- 引擎里**不要再写内联布局样式**（`display` / `flex-direction` / 定位），否则媒体查询失效。
- 新增会改 `gameState` 的引擎级全局状态 → 想清楚它要不要进存档（参照 `_reactiveState` 的教训）。
- 改 `_variables` 里的字段名/结构后，老存档靠 `SAVE_VERSION` 兜底：结构不兼容时把版本号 +1。
