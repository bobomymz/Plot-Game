现状盘点：已有的四代测试资产

┌─────┬──────────────────────────────────────────────┬─────────────────────────────────────────────────────┬────────────────────────────────┐
│ 代  │                     工具                     │                       干什么                        │              形态              │
├─────┼──────────────────────────────────────────────┼─────────────────────────────────────────────────────┼────────────────────────────────┤
│ 1   │ verify_xindahui.py                           │ 关键词 grep 验证                                    │ 一次性脚本                     │
├─────┼──────────────────────────────────────────────┼─────────────────────────────────────────────────────┼────────────────────────────────┤
│ 2   │ check_jianping.js / check_renji.js           │ vm                                                  │ 每区域复制一份，文件列表硬编码 │
│     │                                              │ 加载全量剧情，查死链/条件报错/函数抛错/空text/孤岛  │                                │
├─────┼──────────────────────────────────────────────┼─────────────────────────────────────────────────────┼────────────────────────────────┤
│ 3   │ graph_audit.mjs + ai_phrase_scan*.py +       │ 图结构审计 / 文风扫描 / 体力收支审计                │ 通用，可传参                   │
│     │ stamina_audit.py                             │                                                     │                                │
├─────┼──────────────────────────────────────────────┼─────────────────────────────────────────────────────┼────────────────────────────────┤
│ 4   │ stamina_telemetry_selftest.js                │ 引擎遥测段的自测（node 里跑浏览器桩）               │ 单点自测                       │
└─────┴──────────────────────────────────────────────┴────────────────────────┴────────────────────────────────┘

明显的演化轨迹：一次性 grep → 按区域复制的静态检查 → 通用 和 check_renji 是同一份代码改两个文件名——泛化合并成lint_story.mjs 是欠着的债。

另外挖到两个测试杠杆，engine.js 里现成的：

1. gameState 可从页面直接读写——顶层 let gameState = {}，Playwright 的 page.evaluate(() => gameState._currentAnswer)
   能直接访问。这意味着测试可以：任意传送场景、改时间躲夜  oracle、直接设置 hasXxx走特定分支。不用给引擎加任何测试钩子。
2. 体力遥测系统（__wrapState 代理）——每次体力变动都带来源4000条）。Playwright 走查完把 __staminaLog倒出来，就能精确定位“这一局体力异常扣在哪个节点的哪一行”。这是现成的运行时探针，stamina_audit.py 已经在消费它。

分类提案：两轴定一份“改了什么 → 跑什么”的决策表

用户感知的“测试需求”其实是改动类型，工具选择是层级，skill 的核心就是把两个轴钉死：

                 ┌─ L1 静态体检（秒级，node）────────── 死链/条件报错/抛错/空text/图片404/经济一致性
                 ├─ L2 图结构（已有 graph_audit）──────  timization skill，不重复
改动类型 ────────┼─ L3 无头模拟（vm 里真执行）───────── 走查+不变量：体力0-10、itemCount≤bagVolume、卡死检测
                 └─ L4 浏览器E2E（Playwright，最贵）───   视口

改了剧情数据     → L1（全图）+ L2（该区域）
改了引擎         → stamina_telemetry_selftest + L4 冒烟
动了数值/物品    → L1 经济一致性 + L3 不变量
大改/发布前      → 全家桶 + L4 主线走通

L3 是目前最大缺口：graph_audit 只用 5 个状态变体静态提边，不执行 effects、不跑 reactive rules、不触发 globalTriggers。“条件全对但 effects 里把
itemCount 加成负数”这类错，只有真执行才抓得到。但 L3 要在→applyReactive→再钳位 管线（漏一步就误报），工作量最大。

坑点清单（skill 的主菜，草稿）

L4 Playwright 坑（最密集，全是本项目特有）：

┌─────────────────────────────────────────────┬─────────────────────────────────┐
│                     坑                      │                             对策                             │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ file:// 被拦                                │ 先起 http-server（8630）                                     │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ 选项每次渲染都 Fisher-Yates 乱序            │ 永远按文字选不按位置；文字还是函数动态+插值后的 → 用包含匹配 │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ 打字机逐字显示                              │ 等“选项按钮出现”而非“文字相等”；点文本区跳过；分段文本逐段点 │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ QTE 进场景即倒计时                          │ 慢一步被 timeoutScene 跳走 → 要么快，要么故意测超时分支      │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ 记忆闪色动画阻塞选项                        │ 等 _seqPlayed；答案可从 gameState._currentAnswer 直接读      │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ localStorage 自动存档                       │ 新 context 或先点“重新开始”，否则接着上次的进度玩            │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ 天黑强制过夜拦截长走查                      │ 直接写 gameState.hh = 10 控时绕开                            │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ Math.random（武器断/尸潮）                  │ 接受非确定性，或 page.evaluate 里 stub 掉 Math.random        │
├─────────────────────────────────────────────┼─────────────────────────────────┤
│ 条件求值 ReferenceError 被 catch 吞成 false │ 收集 console 报错，当失败报，不许当良性                      │
└─────────────────────────────────────────────┴─────────────────────────────────┘

L1 静态坑： 条件求值错误被吞（同上，静态就该抓）；函数型  态漏分支（check_jianping 只用初始态——它和 graph_audit的差异本身就是坑）；set hasXxx 不 add itemCount 的经济失衡；webp 化后引用残留 .png/.jpg（image-map.json
就是转换现场的证据）；文本规范（""引号、无 \n\n）可自动查

L3 模拟坑： Set 序列化要用 __set 复原（现有 mkState 模式  ；vm 缺 localStorage/document 要打桩（遥测自测踩过）。

我建议的 skill 形态

.claude/skills/story-testing/SKILL.md
  ├─ 决策表（改动类型 → 测试组合）+ 坑点清单 + 判读标准（真失败 vs 良性）
  ├─ L1: tools/lint_story.mjs      [需建：泛化 check_jian
  ├─ L2: 引用 graph_audit（已有，不碰）
  ├─ L3: tools/sim_walk.mjs        [需建：最大缺口，可选]
  └─ L4: Playwright 配方           [需建：起服务/按文字选/控时/读状态/收console]

和 geo-optimization 同构：脚本做客观检查，skill 存“怎么判读 + 什么时候跑哪个”。

三个想听你想法的问题：

1. L3 无头模拟这轮要不要建？ 它是缺口最大的，但也是工作量最大的。一个折中：先不做完整模拟器，L4 的 Playwright
   走查已经“真执行”（毕竟跑的就是真引擎），用 gameState   部分价值。
2. L4 固化成什么形态：成品脚本（tools/e2e_walk.mjs --scene X --path "选项A→选项B"，稳定但死板）还是 SKILL.md 里的配方代码块（AI 现场按需写
   Playwright 脚本，灵活但每次生成）？我倾向配方 + 一个共选按钮/收错误这些不变的部分固化，路径逻辑现场写）。
3. lint_story.mjs 泛化算不算这个 skill 的份内事？算的话，check_jianping/check_renji 两个旧脚本就退役了。

这次讨论收敛后，和上次 geo-optimization 一样直接建 .claude/skills/（不走 OpenSpec change），你觉得可以就说一声。

✻ Brewed for 2m 57s · done 17:28

❯ 三个问题我觉得就先按你的想法来。另外我觉得L4有个值得讨论的点：截图的token开销大，尽量不用这个功能而是用Playwright MCP 原生 Accessibility
  Snapshot，有什么非要截图才能干的活交给我来测试反馈就行。

  Thought for 57s

三点收到。截图这点你提得比我原方案好，而且它其实能推广成 优先于像素断言。展开说：

无截图验证的三层法

这个游戏的 DOM 结构对无截图测试异常友好，几乎每类“看起来

┌───────────────────┬────────────────────────────────────────────────────────────────────────────────────┐
│      想验证       │                 不用截图怎么做                 │                              依据                              │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 剧情文本对不对    │ a11y snapshot 里的 #scene-text                 │ 按钮全是真 <button> + textContent，a11y 树完整捕获             │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 选项列表/守卫生效 │ a11y snapshot 里 .choice-btn 的文字集合        │ showCondition 为假的选项不渲染 → 树里没有 → 断言即得           │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 输入框选项        │ 树里的 textbox + label + “确认”按钮            │ .choice-input-container 语义完整                               │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 元素可见性        │ a11y 树的缺席即断言                            │ display:none（🔍角标、回溯按钮）不进 a11y 树——白送的可见性检查 │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 屏幕特效是否激活  │ page.evaluate(() => overlay.className)         │ 暗角/雨/丧尸遮罩全是 class 切换，不进 a11y 树但 DOM 一查便知   │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 记忆闪色播完没    │ gameState._seqPlayed                           │ 状态即真相                                                     │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 图片 404          │ network listener 收 requestfailed/404          │ 像素级的“图对不对”才需要人眼                                   │
├───────────────────┼────────────────────────────────────────────────────────────────────────────────────┤
│ 体力异常归因      │ localStorage 里的 __staminaLog（带文件：行号） │ 现成遥测                                                       │
└───────────────────┴────────────────────────────────────────────────────────────────────────────────────┘

两个 a11y snapshot 特有的坑，会写进 skill：

1. 打字机效果期间 snapshot 拿到的是半截文本——先点一下文本 再 snapshot，否则断言 text contains 会误报。
2. QTE 进度条没无障碍语义，树里看不见——但 QTE 的行为（超时自动跳 timeoutScene）本来就该用跳转结果断言，不需要看见条。

L4 因此分成两个模式（都不碰截图）

脚本模式（回归走查）: test_helper.mjs + 现场写的路径脚本 → node 跑 → 纯文本摘要输出
  便宜、可重复、一次跑几十个场景；console错误/404/卡死/不
MCP 模式（交互调试）: Playwright MCP + a11y snapshot
  适合"看看这个场景现在什么样"、现场排查；一次一场景，tok

值得说破的一层：MCP 模式里每次 snapshot 也是全量树回传， 所以默认走脚本模式（输出只有汇总+失败明细），MCP只留给单点排查——这个分级本身就是坑点清单的一条。

交给你的部分：人工验收单

真需要人眼的活固化成一张任务单模板，skill 每次按需生成：

┌──────────┬───────────────────────────────────────────────────────┐
│   类别   │                         内容
├──────────┼───────────────────────────────────────────────────────┤
│ 图片内容 │ 生成的图是否贴合场景（AI 不看截图，图对不对
├──────────┼───────────────────────────────────────────────────────┤
│ 布局观感 │ 溢出、错位、手机端 42dvh 收缩是否自然
├──────────┼───────────────────────────────────────────────────────┤
│ 特效观感 │ 暗角强度/雨滴/丧尸剪影的密度和位置
├──────────┼───────────────────────────────────────────────────────┤
│ 手势体感 │ 图片查看器双指缩放、拖拽跟手度
├──────────┼───────────────────────────────────────────────────────┤
│ 节奏感   │ 打字机速度、分段停顿、QTE 时限松紧
└──────────┴───────────────────────────────────────────────────────┘

每条附快速到达方式（gameState 传送代码片段 + 需要的前置 flag），你打开浏览器两分钟验完，不用自己从头玩过去。

定稿形态

.claude/skills/story-testing/SKILL.md
  ├─ 决策表（改动类型 → L1/L2/L4组合）+ 坑点清单 + 判读标
  ├─ 无截图三层断言法 + a11y snapshot 两坑 + 脚本/MCP分级
  ├─ L1: tools/lint_story.mjs     [建] 泛化 check_jianpin 脚本退役
  ├─ L2: 引用 graph_audit（不重建）
  ├─ L4: tools/test_helper.mjs    [建] 起服务/按文字选按 遥测
  ├─ L3: 暂缓（L4真引擎执行+状态断言近似覆盖）
  └─ 人工验收单模板（含 gameState 传送片段）

设计上没有悬而未决的了。确认的话我就退出探索模式开建——顺 lper.mjs拿真实场景验证一遍（起服务、按文字选、控时、收错误），校准过的坑再写进 SKILL.md，最后 lint_story.mjs 泛化 + 退役旧脚本 + CLAUDE.md 索引加一行。